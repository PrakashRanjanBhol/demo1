isOpen = false;         // initially closed
  isFullWidth = false;    // controls collapsed/expanded

  toggleWidth() {
    this.isFullWidth = !this.isFullWidth;
  }

  closeSidebar() {
    this.isOpen = false;
  }

  openSidebar() {
    this.isOpen = true;
  }
