import React, { useState } from 'react';
import { Info } from 'lucide-react';

const InfoCard = () => {
  const [isHovered, setIsHovered] = useState(false);

  return (
    <div className="max-w-md mx-auto mt-8">
      <div className="bg-white rounded-lg shadow-lg border border-gray-200 p-6 relative">
        {/* Info Icon */}
        <div 
          className="absolute top-4 right-4"
          onMouseEnter={() => setIsHovered(true)}
          onMouseLeave={() => setIsHovered(false)}
        >
          <Info 
            className="w-5 h-5 text-gray-400 hover:text-blue-500 cursor-help transition-colors duration-200" 
          />
          
          {/* Tooltip */}
          {isHovered && (
            <div className="absolute top-6 right-0 w-72 bg-gray-800 text-white text-sm rounded-lg p-3 shadow-lg z-10 transform -translate-x-4">
              <div className="relative">
                {/* Arrow pointing up to the icon */}
                <div className="absolute -top-6 right-6 w-0 h-0 border-l-4 border-r-4 border-b-4 border-l-transparent border-r-transparent border-b-gray-800"></div>
                
                <h4 className="font-semibold mb-2 text-blue-200">How to use this card:</h4>
                <ul className="space-y-1 text-xs">
                  <li>• Click on the card to view full details</li>
                  <li>• Use the action buttons at the bottom for quick operations</li>
                  <li>• Bookmark this card by clicking the star icon</li>
                  <li>• Share the card using the share button</li>
                  <li>• Double-click to edit if you have permissions</li>
                </ul>
                
                <div className="mt-2 pt-2 border-t border-gray-600 text-xs text-gray-300">
                  💡 Pro tip: Right-click for additional options
                </div>
              </div>
            </div>
          )}
        </div>

        {/* Card Content */}
        <div className="pr-8">
          <h3 className="text-xl font-bold text-gray-800 mb-2">Sample Card Title</h3>
          <p className="text-gray-600 mb-4">
            This is a sample card with an info icon in the top-right corner. 
            Hover over the info icon to see usage instructions.
          </p>
          
          <div className="flex flex-wrap gap-2 mb-4">
            <span className="px-3 py-1 bg-blue-100 text-blue-800 text-sm rounded-full">Feature</span>
            <span className="px-3 py-1 bg-green-100 text-green-800 text-sm rounded-full">Active</span>
          </div>
          
          <div className="flex space-x-2">
            <button className="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition-colors">
              Primary Action
            </button>
            <button className="px-4 py-2 border border-gray-300 text-gray-700 rounded hover:bg-gray-50 transition-colors">
              Secondary
            </button>
          </div>
        </div>
      </div>
    </div>
  );
};

export default InfoCard;
