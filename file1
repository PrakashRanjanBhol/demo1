/* Update .tree-line::before: */
.tree-line::before {
    content: '';
    position: absolute;
    top: 20px;
    left: 0;
    width: 12px;
    height: 1px;
    background: var(--adk-agents-border);
}

/* Update .tree-line.last: */
.tree-line.last {
    height: 20px;
}
