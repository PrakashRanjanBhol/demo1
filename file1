<div class="date-range-filter">
    <div class="date-range-header">
        <i class="pi pi-calendar date-icon"></i>
        <span class="date-label">Select a Date Range</span>
    </div>

    <div class="date-picker-row">
        <p-calendar [(ngModel)]="selectedRange" selectionMode="range" dateFormat="dd-mm-yy" placeholder="Choose dates"
            [maxDate]="maxDate" [minDate]="minDate" [readonlyInput]="true" class="custom-calendar"></p-calendar>

        <button pButton type="button" label="Apply" icon="pi pi-check" class="apply-btn" (click)="applyDateRange()"
            [disabled]="!selectedRange || selectedRange.length < 2"></button>
    </div>
</div>

<!-- 🔹 Show only applied period -->
<div *ngIf="appliedRange?.length === 2" class="period-header">
    <i class="pi pi-clock period-icon"></i>
    <span class="period-label">Selected Period:</span>
    <span class="period-value">
        {{ appliedRange[0] | date: 'dd-MM-yyyy' }} → {{ appliedRange[1] | date: 'dd-MM-yyyy' }}
    </span>
</div>

<div echarts [options]="chartOption" class="chart"></div>



/* Centered Container */
.date-range-filter {
  display: flex;
  flex-direction: column;
  align-items: center;
  /* ✅ centers horizontally */
  text-align: center;
  margin-bottom: 1rem;
  font-family: 'Poppins', sans-serif;
  background: #1a1a1d;
  padding: 1.2rem;
  border-radius: 12px;
  border: 1px solid #2a2f3a;
  box-shadow: 0px 3px 8px rgba(0, 0, 0, 0.35);
}

/* Label Section */
.date-range-header {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-bottom: 10px;
}

.date-label {
  font-size: 15px;
  font-weight: 600;
  color: #e0e6f0;
}

.date-icon {
  font-size: 18px;
  color: #42a5f5;
}

/* Picker + Apply button row */
.date-picker-row {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
}

/* Calendar customization */
.custom-calendar .p-inputtext {
  background: #1c1f26 !important;
  color: #ffffff !important;
  border: 1px solid #2a2f3a !important;
  border-radius: 6px !important;
  padding: 0.6rem !important;
  font-size: 14px;
  min-width: 200px;
  text-align: center;
  /* center text inside input */
}

.custom-calendar .p-inputtext:focus {
  border-color: #42a5f5 !important;
  box-shadow: 0 0 6px rgba(66, 165, 245, 0.5) !important;
}

/* Apply button */
.apply-btn {
  border-radius: 6px !important;
  padding: 0.6rem 1rem !important;
  background: linear-gradient(135deg, #42a5f5, #1e88e5) !important;
  border: none !important;
  font-weight: 600;
  box-shadow: 0px 3px 8px rgba(0, 0, 0, 0.3);
  transition: all 0.25s ease;
}

.apply-btn:hover {
  background: linear-gradient(135deg, #64b5f6, #2196f3) !important;
  transform: translateY(-2px);
}

/* Selected Period Header */
.period-header {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  background: #1a1a1d;
  border: 1px solid #2a2f3a;
  border-radius: 10px;
  padding: 0.8rem 1.2rem;
  margin: 1rem auto;
  width: fit-content;
  /* ✅ keeps it centered */
  font-family: 'Poppins', sans-serif;
  color: #ffffff;
  font-size: 14px;
  font-weight: 500;
  box-shadow: 0px 3px 8px rgba(0, 0, 0, 0.35);
  transition: all 0.3s ease;
}

.period-header:hover {
  box-shadow: 0px 5px 14px rgba(0, 0, 0, 0.45);
  transform: translateY(-2px);
}

.period-icon {
  font-size: 16px;
  color: #42a5f5;
}

.period-label {
  font-weight: 600;
  color: #aab4c3;
}

.period-value {
  font-weight: 600;
  color: #ffffff;
}

