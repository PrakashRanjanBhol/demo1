<div class="jira-signin-container" data-theme="dark">
  <p class="signin-label">Sign in with</p>
  <div class="jira-content">
    <div class="jira-icon">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
        <path d="M12 2L2 7V17L12 22L22 17V7L12 2Z" fill="white" fill-opacity="0.9"/>
      </svg>
    </div>
    <div class="jira-text">
      <p class="jira-title">Jira</p>
      <p class="jira-subtitle">Atlassian Account</p>
    </div>
  </div>
</div>



/* Dark Mode Variables (Default) */
:root,
[data-theme="dark"] {
  --bg-primary: #111113;
  --bg-secondary: #1a1a1d;
  --border-color: #2a2a2d;
  --text-primary: #ffffff;
  --text-secondary: #8B92A8;
  --text-tertiary: #5a5a5f;
  --jira-blue: #0052CC;
}

/* Light Mode Variables */
[data-theme="light"] {
  --bg-primary: #ffffff;
  --bg-secondary: #f5f5f5;
  --border-color: #e0e0e0;
  --text-primary: #1a1a1d;
  --text-secondary: #5a5a5f;
  --text-tertiary: #8B92A8;
  --jira-blue: #0052CC;
}

.jira-signin-container {
  margin-bottom: 24px;
  padding: 16px;
  border-radius: 8px;
  border: 1px solid var(--border-color);
  background-color: var(--bg-secondary);
}

.signin-label {
  font-size: 12px;
  margin-bottom: 12px;
  color: var(--text-secondary);
  margin-top: 0;
}

.jira-content {
  display: flex;
  align-items: center;
  gap: 12px;
}

.jira-icon {
  width: 40px;
  height: 40px;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: var(--jira-blue);
  flex-shrink: 0;
}

.jira-text {
  flex: 1;
}

.jira-title {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0 0 2px 0;
}

.jira-subtitle {
  font-size: 12px;
  color: var(--text-secondary);
  margin: 0;
}
