.scrollWrapper {
    max-height: 400px; /* Adjust as needed */
    overflow-y: auto;
    border: 1px solid #2a2a2a;
    padding-right: 4px; /* for scrollbar space */
    margin-bottom: 1rem;
    border-radius: 6px;
}


<div className={styles.scrollWrapper}>
  <div className={styles.spaceList}>
    {spaces.map(space => (
      // existing space render logic here...
    ))}
  </div>
</div>
