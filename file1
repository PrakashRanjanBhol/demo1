<!-- Updated Status View Section - Replace the Status View section in your pr-container -->

<!-- Status View -->
<ng-container *ngIf="currentSort === 'status' && filteredAndSortedPRs.length > 0">
  <ng-container *ngIf="currentOrder === 'open'">
    <!-- Open PRs Section -->
    <div class="section-header">Open ({{ openPRs.length }})</div>
    
    <ng-container *ngIf="openPRs.length > 0">
      <div 
        class="pr-card" 
        [class.selected]="selectedPRId === pr.id"
        *ngFor="let pr of openPRs"
        (click)="selectPR(pr)">
        <!-- PR card content here -->
      </div>
    </ng-container>
    
    <div class="section-empty-state" *ngIf="openPRs.length === 0">
      <svg class="section-empty-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <span>No Open PRs</span>
    </div>

    <!-- Merged PRs Section -->
    <div class="section-header">Merged ({{ mergedPRs.length }})</div>
    
    <ng-container *ngIf="mergedPRs.length > 0">
      <div 
        class="pr-card" 
        [class.selected]="selectedPRId === pr.id"
        *ngFor="let pr of mergedPRs"
        (click)="selectPR(pr)">
        <!-- PR card content here -->
      </div>
    </ng-container>
    
    <div class="section-empty-state" *ngIf="mergedPRs.length === 0">
      <svg class="section-empty-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
      <span>No Merged PRs</span>
    </div>
  </ng-container>

  <ng-container *ngIf="currentOrder === 'merged'">
    <!-- Merged PRs Section -->
    <div class="section-header">Merged ({{ mergedPRs.length }})</div>
    
    <ng-container *ngIf="mergedPRs.length > 0">
      <div 
        class="pr-card" 
        [class.selected]="selectedPRId === pr.id"
        *ngFor="let pr of mergedPRs"
        (click)="selectPR(pr)">
        <!-- PR card content here -->
      </div>
    </ng-container>
    
    <div class="section-empty-state" *ngIf="mergedPRs.length === 0">
      <svg class="section-empty-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
      <span>No Merged PRs</span>
    </div>

    <!-- Open PRs Section -->
    <div class="section-header">Open ({{ openPRs.length }})</div>
    
    <ng-container *ngIf="openPRs.length > 0">
      <div 
        class="pr-card" 
        [class.selected]="selectedPRId === pr.id"
        *ngFor="let pr of openPRs"
        (click)="selectPR(pr)">
        <!-- PR card content here -->
      </div>
    </ng-container>
    
    <div class="section-empty-state" *ngIf="openPRs.length === 0">
      <svg class="section-empty-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <span>No Open PRs</span>
    </div>
  </ng-container>
</ng-container>

















/* Add these styles for section empty state */

.section-empty-state {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 24px 16px;
  background: var(--bitbucket-prs-bg-tertiary);
  border: 1px dashed var(--bitbucket-prs-border-primary);
  border-radius: 8px;
  margin-bottom: 12px;
  color: var(--bitbucket-prs-text-muted);
  font-size: 13px;
  font-weight: 500;
  font-style: italic;
  
  /* Animation */
  animation: fadeInUp 0.4s ease-out forwards;
  opacity: 0;
}

.section-empty-icon {
  width: 20px;
  height: 20px;
  color: var(--bitbucket-prs-text-muted);
  opacity: 0.6;
  flex-shrink: 0;
}

/* Optional: Different styling for each state */
.section-empty-state:has(.section-empty-icon circle[cx="18"]) {
  /* Open PR empty state - slightly green tint */
  border-color: var(--bitbucket-prs-state-open-text);
  background: rgba(34, 197, 94, 0.03);
}

.section-empty-state:has(.section-empty-icon path[d*="M5 13l4 4L19 7"]) {
  /* Merged PR empty state - slightly purple tint */
  border-color: var(--bitbucket-prs-state-merged-text);
  background: rgba(139, 92, 246, 0.03);
}

/* Keep existing section header styles */
.section-header {
  font-size: 13px;
  font-weight: 600;
  color: var(--bitbucket-prs-text-muted);
  margin: 20px 0 12px 0;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  
  /* Add animation */
  animation: fadeInUp 0.3s ease-out forwards;
  opacity: 0;
}

.section-header:first-of-type {
  margin-top: 0;
}
