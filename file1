/* Update file info section */
.file-info-section {
  padding: 16px 24px;
  background: var(--bitbucket-file-bg-secondary);
  border-top: 1px solid var(--bitbucket-file-border-primary);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
}

.lines-info {
  font-size: 13px;
  color: var(--bitbucket-file-text-tertiary);
  font-weight: 500;
}

.download-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  background: var(--bitbucket-file-gradient-accent);
  color: #ffffff;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 500;
  transition: all 0.3s ease;
  box-shadow: 0 2px 8px var(--bitbucket-file-shadow-medium),
              0 1px 2px var(--bitbucket-file-shadow-medium-alt);
  flex-shrink: 0; /* Add this to prevent button from shrinking */
}

.download-btn:hover {
  background: var(--bitbucket-file-gradient-accent-hover);
  transform: translateY(-2px);
  box-shadow: 0 4px 12px var(--bitbucket-file-shadow-high),
              0 2px 4px var(--bitbucket-file-shadow-high-alt);
}

.download-btn svg {
  width: 16px;
  height: 16px;
  flex-shrink: 0;
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .file-info-section {
    flex-direction: column;
    align-items: stretch;
  }
  
  .download-btn {
    width: 100%;
    justify-content: center;
  }
}
