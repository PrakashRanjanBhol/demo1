<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thinking Emoji Animation</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #090d16;
            overflow: hidden;
            color: #fff;
            font-family: "Poppins", sans-serif;
        }

        .thinking {
            position: relative;
            font-size: 6rem;
            margin-bottom: 20px;
        }

        .emoji {
            display: inline-block;
            animation: nod 2s infinite ease-in-out;
            transform-origin: center bottom;
        }

        @keyframes nod {

            0%,
            100% {
                transform: rotate(0deg);
            }

            20% {
                transform: rotate(8deg);
            }

            40% {
                transform: rotate(-6deg);
            }

            60% {
                transform: rotate(4deg);
            }

            80% {
                transform: rotate(-3deg);
            }
        }

        /* Floating stars / sparkles */
        .star {
            position: absolute;
            font-size: 1.5rem;
            opacity: 0;
            animation: float 4s infinite;
        }

        .star:nth-child(2) {
            top: -20px;
            left: 80px;
            animation-delay: 0.5s;
        }

        .star:nth-child(3) {
            top: -60px;
            left: -40px;
            animation-delay: 1s;
        }

        .star:nth-child(4) {
            top: -80px;
            left: 40px;
            animation-delay: 1.5s;
        }

        .star:nth-child(5) {
            top: -30px;
            left: -70px;
            animation-delay: 2s;
        }

        @keyframes float {
            0% {
                transform: translateY(0) scale(0.5);
                opacity: 0;
            }

            20% {
                opacity: 1;
            }

            50% {
                transform: translateY(-40px) scale(1);
                opacity: 1;
            }

            100% {
                transform: translateY(-80px) scale(0.5);
                opacity: 0;
            }
        }

        /* Caption text */
        .caption {
            font-size: 1.3rem;
            text-align: center;
            color: #e0e7ff;
            text-shadow: 0 0 8px rgba(99, 102, 241, 0.8);
            animation: fadeIn 2s ease-in-out infinite alternate;
        }

        @keyframes fadeIn {
            from {
                opacity: 0.6;
            }

            to {
                opacity: 1;
            }
        }
    </style>
</head>

<body>
    <div class="thinking">
        <div class="emoji">🤔</div>
        <div class="star">✨</div>
        <div class="star">🌟</div>
        <div class="star">💫</div>
        <div class="star">✨</div>
        <div class="star">🌟</div>
    </div>
    <div class="caption">Thinking of a refreshing greeting to start your day</div>
</body>

</html>
