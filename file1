/* Add this style back: */
.accordion-title-container {
    display: flex;
    flex-direction: column;
    gap: 4px;
}

.accordion-subtitle {
    font-size: 12px;
    font-weight: 400;
    color: var(--adk-agents-text-secondary);
}
