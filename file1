 const options = {
    title: {
        text: `Trend - ${selectedType}`,
        left: 'center',
        textStyle: {
            color: '#fff' // Title color
        }
    },
    tooltip: {
        trigger: 'axis',
        textStyle: {
            color: '#fff' // Tooltip text
        },
        backgroundColor: '#333'
    },
    legend: {
        textStyle: {
            color: '#fff' // Legend labels
        }
    },
    xAxis: {
        type: 'category',
        name: xKey,
        data: xData,
        nameTextStyle: {
            color: '#fff'
        },
        axisLabel: {
            color: '#fff'
        },
        axisLine: {
            lineStyle: {
                color: '#888'
            }
        }
    },
    yAxis: {
        type: 'value',
        name: 'Count',
        nameTextStyle: {
            color: '#fff'
        },
        axisLabel: {
            color: '#fff'
        },
        axisLine: {
            lineStyle: {
                color: '#888'
            }
        },
        splitLine: {
            lineStyle: {
                color: '#444'
            }
        }
    },
    series: [
        {
            data: yData,
            type: 'line',
            smooth: true,
            lineStyle: {
                color: '#42A5F5'
            },
            itemStyle: {
                color: '#42A5F5'
            }
        }
    ],
};









const chartOption = {
    title: {
        text: `Scatter Plot - ${selectedType}`,
        left: 'center',
        textStyle: {
            color: '#fff' // White title
        }
    },
    legend: {
        top: 30,
        textStyle: {
            color: '#fff' // White legend
        }
    },
    tooltip: {
        trigger: 'item',
        backgroundColor: '#333', // Dark background for tooltip
        textStyle: {
            color: '#fff' // White tooltip text
        },
        formatter: params =>
            `Category: ${params.seriesName}<br/>X: ${params.data[0]}<br/>Y: ${params.data[1]}`
    },
    xAxis: {
        type: 'value',
        name: 'X Coord',
        nameTextStyle: {
            color: '#fff' // Axis name
        },
        axisLabel: {
            color: '#fff' // Axis labels
        },
        axisLine: {
            lineStyle: {
                color: '#888' // Axis line color
            }
        },
        splitLine: {
            lineStyle: {
                type: 'dashed',
                color: '#444'
            }
        }
    },
    yAxis: {
        type: 'value',
        name: 'Y Coord',
        nameTextStyle: {
            color: '#fff'
        },
        axisLabel: {
            color: '#fff'
        },
        axisLine: {
            lineStyle: {
                color: '#888'
            }
        },
        splitLine: {
            lineStyle: {
                type: 'dashed',
                color: '#444'
            }
        }
    },
    series,
    backgroundColor: '#181818' // Optional dark background
};
