<!-- OLD -->
<span class="info-badge-label">Path:</span> {{ selectedFile.path }}{{ selectedFile.name }}

<!-- NEW -->
<span class="info-badge-label">Path:</span> 
<span class="file-path-text">{{ selectedFile.path }}{{ selectedFile.name }}</span>



/* File path with ellipsis */
.file-path-text {
  max-width: 300px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  display: inline-block;
  vertical-align: bottom;
}

/* Adjust for smaller screens */
@media (max-width: 768px) {
  .file-path-text {
    max-width: 150px;
  }
}
