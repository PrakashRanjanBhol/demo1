.sidebar-item {
  padding: 8px 10px;
  cursor: pointer;
  user-select: none;
  display: flex;
  align-items: center;
  gap: 8px;
  border-radius: 4px;
  margin: 2px 0;
  transition: background-color 0.2s;
  color: var(--code-rag-documentation-text-primary);
  font-size: 0.8125rem; /* 13px */
  line-height: 1.5;
}

.sidebar-item .icon {
  width: 16px;
  height: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.9375rem; /* 15px */
  flex-shrink: 0;
}

.sidebar-item .icon svg {
  display: block;
  color: var(--code-rag-documentation-text-secondary);
}

.sidebar-item.folder .icon svg {
  color: var(--code-rag-documentation-text-link);
}

.sidebar-item:hover .icon svg {
  color: var(--code-rag-documentation-text-primary);
}

.sidebar-item.selected {
  background-color: var(--code-rag-documentation-bg-hover);
  border-left: 3px solid var(--code-rag-documentation-text-link);
  padding-left: 7px;
}

.sidebar-item.selected .icon svg {
  color: var(--code-rag-documentation-text-link);
}
