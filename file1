/* Update .version-node: */
.version-node {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 8px 10px;
    background: transparent;
    border: none;
    transition: all 0.2s;
}

.version-node:hover {
    background: var(--adk-agents-card-hover-bg);
    border-radius: 6px;
}

/* Update .btn-spinner: */
.btn-spinner {
    width: 16px;
    height: 16px;
    border: 2px solid transparent;
    border-top-color: currentColor;
    border-right-color: currentColor;
    border-radius: 50%;
    animation: spin 0.6s linear infinite;
}

.download-btn:disabled .btn-spinner {
    border-top-color: var(--adk-agents-text-secondary);
    border-right-color: var(--adk-agents-text-secondary);
}
