.sidebar-content {
  padding: 1.5rem;
  overflow-y: auto;
  overflow-x: hidden;
  flex: 1;
  display: flex;
  flex-direction: column;
}

.task-form-container {
  opacity: 0;
  visibility: hidden;
  max-height: 0;
  overflow: hidden;
  transform: translateY(-0.625rem);
  transition: opacity 0.3s ease, transform 0.3s ease, max-height 0.3s ease, visibility 0s linear 0.3s, margin 0.3s ease;
  margin-top: 0;
  margin-bottom: 0;
  width: 100%;
}

.task-form-container.show {
  opacity: 1;
  visibility: visible;
  max-height: 500px;
  transform: translateY(0);
  transition: opacity 0.3s ease, transform 0.3s ease, max-height 0.3s ease, visibility 0s linear 0s, margin 0.3s ease;
  margin-top: 0;
  margin-bottom: 1.5rem;
}

.tasks-section {
  margin-top: 0;
  width: 100%;
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.tasks-list {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  transition: opacity 0.3s ease 0.1s;
}

.task-card {
  background: var(--sdlc-suite-bg-card);
  border: 1px solid var(--sdlc-suite-border-secondary);
  border-radius: 0.75rem;
  padding: 1rem;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  transition: transform 0.3s ease, border-color 0.3s ease;
  animation: slideInCard 0.4s ease-out backwards;
}

@keyframes slideInCard {
  from {
    opacity: 0;
    transform: translateY(0.625rem);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.task-card:nth-child(1) { animation-delay: 0.05s; }
.task-card:nth-child(2) { animation-delay: 0.1s; }
.task-card:nth-child(3) { animation-delay: 0.15s; }
.task-card:nth-child(4) { animation-delay: 0.2s; }
.task-card:nth-child(5) { animation-delay: 0.25s; }
