/* ✅ NEW - NO BLINKING */
.file-item {
    padding: 12px 16px;
    margin: 4px 8px;
    display: flex;
    align-items: center;
    gap: 12px;
    border-radius: 6px;
    transition: background-color 0.2s ease;
    cursor: default;
}

.file-item.clickable {
    cursor: pointer;
}

/* Single hover rule - no conflicts */
.file-item:hover {
    background-color: var(--bitbucket-bg-tertiary);
}

.file-icon {
    width: 20px;
    height: 20px;
    flex-shrink: 0;
}

.folder-icon {
    color: var(--bitbucket-blue);
}

.file-icon-default {
    color: var(--bitbucket-text-secondary);
}

.file-name {
    font-size: 13px;
    color: var(--bitbucket-text-primary);
    flex: 1;
    word-break: break-word;
}
