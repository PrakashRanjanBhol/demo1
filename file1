const option = {
  backgroundColor: '#090d16',
  tooltip: { trigger: 'item' },
  legend: {
    textStyle: { color: '#fff' },
    top: 10,
    data: [selectedType, 'Line Defect'] // must match `series.name`
  },
  xAxis: { type: 'value' },
  yAxis: { type: 'value' },
  series: [...scatterSeries, ...lineSeries]
};
