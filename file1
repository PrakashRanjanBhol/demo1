async fetchData() {
  try {
    return await firstValueFrom(this.http.get('your-api-url'));
  } catch (error: any) {
    if (error.status === 403) {
      return { error: 'FORBIDDEN' };
    }
    return { error: 'UNKNOWN_ERROR' };
  }
}
