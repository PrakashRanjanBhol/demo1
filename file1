.sidebar-header {
  padding: 16px 12px;
  background: transparent;
  border-bottom: none;
  position: relative;
}

.sidebar-header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 12px;
  right: 12px;
  height: 1px;
  background: linear-gradient(
    90deg, 
    transparent 0%, 
    var(--code-rag-documentation-border) 20%, 
    var(--code-rag-documentation-border) 80%, 
    transparent 100%
  );
}

.sidebar-title {
  font-weight: 500;
  font-size: 0.8125rem; /* 13px */
  color: var(--code-rag-documentation-text-primary);
  margin-bottom: 10px;
  text-transform: uppercase;
  letter-spacing: 1px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.sidebar-title::before {
  content: '';
  width: 3px;
  height: 14px;
  background: var(--code-rag-documentation-text-link);
  border-radius: 2px;
}

.breadcrumb {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 0.75rem; /* 12px */
  flex-wrap: wrap;
  line-height: 1.6;
  padding: 6px 8px;
  background: var(--code-rag-documentation-bg-hover);
  border-radius: 6px;
  border-left: 2px solid var(--code-rag-documentation-text-link);
}

.breadcrumb-item {
  color: var(--code-rag-documentation-text-secondary);
  cursor: pointer;
  padding: 2px 6px;
  border-radius: 3px;
  transition: all 0.2s;
  position: relative;
}

.breadcrumb-item:hover {
  color: var(--code-rag-documentation-text-link);
  background-color: var(--code-rag-documentation-bg-hover-breadcrumb);
}

.breadcrumb-item.current {
  color: var(--code-rag-documentation-text-primary);
  font-weight: 500;
  cursor: default;
  background-color: var(--code-rag-documentation-bg-primary);
  padding: 2px 8px;
}

.breadcrumb-item.current:hover {
  background-color: var(--code-rag-documentation-bg-primary);
}

.breadcrumb-separator {
  color: var(--code-rag-documentation-text-muted);
  font-size: 0.75rem; /* 12px */
  margin: 0 2px;
}
