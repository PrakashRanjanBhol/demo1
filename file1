

<div class="feedback-loader">
  <div class="emoji-loader">
    <span>💬</span>
    <span>💡</span>
    <span>🐞</span>
    <span></span>
  </div>
  <div class="feedback-message">
    <span class="message-text">Loading community thoughts</span>
    <span class="dot-wave">
      <span>.</span><span>.</span><span>.</span>
    </span>
  </div>
</div>












.feedback-loader {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 1rem;
  background-color: #212121;
  border-radius: 10px;
  color: #ccc;
  animation: fadeIn 0.3s ease-in;
  text-align: center;
}

.emoji-loader {
  display: flex;
  gap: 10px;
  font-size: 28px;
  margin-bottom: 1rem;
}

.emoji-loader span {
  opacity: 0.3;
  transform: scale(0.8);
  animation: emojiPulse 1.4s infinite ease-in-out;
}

.emoji-loader span:nth-child(1) { animation-delay: 0s; }
.emoji-loader span:nth-child(2) { animation-delay: 0.15s; }
.emoji-loader span:nth-child(3) { animation-delay: 0.3s; }
.emoji-loader span:nth-child(4) { animation-delay: 0.45s; }

.feedback-message {
  font-size: 1.1rem;
  color: #eee;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 4px;
}

.message-text {
  font-weight: 600;
}

.dot-wave span {
  display: inline-block;
  font-size: 1.4rem;
  font-weight: 600;
  animation: wave 1.2s infinite ease-in-out;
}

.dot-wave span:nth-child(1) { animation-delay: 0s; }
.dot-wave span:nth-child(2) { animation-delay: 0.2s; }
.dot-wave span:nth-child(3) { animation-delay: 0.4s; }

@keyframes emojiPulse {
  0%, 100% {
    opacity: 0.3;
    transform: scale(0.8);
  }
  50% {
    opacity: 1;
    transform: scale(1.3);
  }
}

@keyframes fadeIn {
  from { opacity: 0; transform: scale(0.95); }
  to { opacity: 1; transform: scale(1); }
}

@keyframes wave {
  0%, 60%, 100% {
    transform: translateY(0);
  }
  30% {
    transform: translateY(-6px);
  }
}
