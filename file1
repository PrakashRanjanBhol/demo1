  const handleToggle = (file_job_id: string) => {
    const newId = selectedJobId === file_job_id ? null : file_job_id;
    setSelectedJobId(newId);
    // TODO: dispatch to store — replace with your actual dispatch/action
    // dispatch(setSelectedFileJobId(newId));
    setError(null);
    setWarning(null);
  };
