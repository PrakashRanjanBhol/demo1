<!-- PR Header -->
<ng-container *ngIf="activeView === 'pr' && selectedPR">
  <div class="breadcrumb animate-in">
    <span class="breadcrumb-badge pr-badge">PR</span>
    <svg class="breadcrumb-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <circle cx="18" cy="18" r="3"/>
      <circle cx="6" cy="6" r="3"/>
      <path d="M13 6h3a2 2 0 0 1 2 2v7"/>
      <line x1="6" y1="9" x2="6" y2="21"/>
    </svg>
    <span>Pull Request</span>
    <svg class="breadcrumb-separator" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 18 15 12 9 6"/>
    </svg>
    <span class="pr-id-link" (click)="onPRIdClick(selectedPR.number)">#{{ selectedPR.number }}</span>
  </div>
  <!-- ... rest remains same ... -->
</ng-container>

<!-- File Header -->
<ng-container *ngIf="activeView === 'file' && selectedFile">
  <div class="breadcrumb animate-in">
    <span class="breadcrumb-badge file-badge">FILE</span>
    <svg class="breadcrumb-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"/>
    </svg>
    <span class="breadcrumb-text">{{ selectedFile.projectName }}</span>
    <svg class="breadcrumb-separator" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 18 15 12 9 6"/>
    </svg>
    <span class="breadcrumb-text">{{ selectedFile.repository }}</span>
    <svg class="breadcrumb-separator" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 18 15 12 9 6"/>
    </svg>
    <span class="breadcrumb-text">File</span>
    <svg class="breadcrumb-separator" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 18 15 12 9 6"/>
    </svg>
    <span class="breadcrumb-text breadcrumb-filename">{{ selectedFile.name }}</span>
  </div>
  <!-- ... rest remains same ... -->
</ng-container>

















/* Breadcrumb badge */
.breadcrumb-badge {
  display: inline-flex;
  align-items: center;
  padding: 3px 8px;
  border-radius: 4px;
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  margin-right: 8px;
}

/* PR badge */
.pr-badge {
  background: var(--bitbucket-file-accent-blue-light);
  color: var(--bitbucket-file-accent-blue);
  border: 1px solid var(--bitbucket-file-accent-blue-border);
}

/* File badge */
.file-badge {
  background: var(--bitbucket-file-accent-purple-light);
  color: var(--bitbucket-file-accent-purple);
  border: 1px solid var(--bitbucket-file-accent-purple-border);
}
