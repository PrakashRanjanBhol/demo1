<!-- Active Workflows Tab -->
<div class="workflow-section">
  <div class="section-header" (click)="toggleSection('activeWorkflows')">
    <div class="section-header-left">
      <div class="section-toggle" [class.collapsed]="activeWorkflowsCollapsed">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
          stroke-linejoin="round">
          <polyline points="6 9 12 15 18 9"></polyline>
        </svg>
      </div>
      <div class="section-title">Active Workflows</div>
      <div class="section-count">{{ activeWorkflows.length }}</div>
    </div>
  </div>
  <div class="section-content" [class.collapsed]="activeWorkflowsCollapsed">
    <!-- Show this when active workflows exist -->
    <div *ngIf="activeWorkflows.length > 0">
      <div *ngFor="let workflow of activeWorkflows" class="workflow-item-readonly">
        <div class="workflow-info">
          <div class="workflow-name">{{ workflow.name }}</div>
          <div class="workflow-dates">
            <div class="workflow-date">
              <span class="workflow-date-label">Created:</span>
              <span class="date-value">{{ formatDate(workflow.createdAt) }}</span>
            </div>
            <div class="workflow-date">
              <span class="workflow-date-label">Updated:</span>
              <span class="date-value">{{ formatDate(workflow.updatedAt) }}</span>
            </div>
          </div>
        </div>
        <div class="workflow-status-badge running">Running</div>
      </div>
    </div>

    <!-- Show this when no active workflows exist -->
    <div *ngIf="activeWorkflows.length === 0" class="empty-active-workflows-state">
      <div class="empty-active-workflows-icon">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
          stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <polyline points="12 6 12 12 16 14"></polyline>
        </svg>
      </div>
      <div class="empty-active-workflows-title">No Active Workflows</div>
      <div class="empty-active-workflows-description">Your running workflows will appear here</div>
    </div>
  </div>
</div>










/* Empty active workflows state */
.empty-active-workflows-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 40px 20px;
    text-align: center;
}

.empty-active-workflows-icon {
    width: 48px;
    height: 48px;
    background: var(--agentic-ai-workflow-bg-icon);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 16px;
}

.empty-active-workflows-icon svg {
    width: 24px;
    height: 24px;
    stroke: var(--agentic-ai-workflow-text-muted);
}

.empty-active-workflows-title {
    font-size: 14px;
    font-weight: 500;
    color: var(--agentic-ai-workflow-text-primary);
    margin-bottom: 6px;
}

.empty-active-workflows-description {
    font-size: 13px;
    color: var(--agentic-ai-workflow-text-muted);
}
