/* Functions header with search and create button */
.functions-header {
    padding: 8px 20px;
    display: flex;
    align-items: center;
    gap: 8px;
    border-bottom: 1px solid var(--agentic-ai-workflow-border-primary);
}

.functions-search-container {
    flex: 1;
}

.functions-search-container .search-input {
    padding: 6px 10px 6px 32px;
    font-size: 13px;
    background-size: 14px;
    background-position: 10px center;
}

.create-function-btn {
    padding: 6px 10px;
    background: var(--agentic-ai-workflow-bg-run-btn);
    color: var(--agentic-ai-workflow-text-white);
    border: none;
    border-radius: 4px;
    font-size: 12px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    gap: 4px;
    white-space: nowrap;
    flex-shrink: 0;
}

.create-function-btn:hover {
    background: var(--agentic-ai-workflow-bg-run-btn-hover);
    transform: translateY(-1px);
}

.create-function-btn:active {
    transform: translateY(0);
}

.create-function-btn svg {
    width: 12px;
    height: 12px;
    stroke: var(--agentic-ai-workflow-text-white);
}
