export class WorkflowSidebarComponent implements OnInit {
  // ... existing properties and methods ...

  // Update executeWorkflow method name to redirectToWorkflow
  redirectToWorkflow(workflowId: string): void {
    this.activeWorkflowId = workflowId;
    console.log(`Redirecting to workflow: ${workflowId}`);
    
    // In production: navigate to workflow URL
    // window.open(`https://your-n8n-instance.com/workflow/${workflowId}`, '_blank');
    // OR
    // this.router.navigate(['/workflow', workflowId]);
    
    alert(`Redirecting to workflow: ${workflowId}\n\nIn production, this will open the workflow editor.`);
  }

  runWorkflow(workflowId: string, event: Event): void {
    event.stopPropagation();
    console.log(`Running workflow: ${workflowId}`);
    alert(`Executing workflow: ${workflowId}\n\nWorkflow execution started.`);
    // In production: make API call to run workflow
  }

  shareWorkflow(workflowId: string, event: Event): void {
    event.stopPropagation();
    console.log(`Sharing workflow: ${workflowId}`);
    alert(`Share Workflow: ${workflowId}\n\nThis will open the share dialog or copy share link.`);
    // In production: open share modal or copy link to clipboard
  }

  // ... rest of existing methods ...
}












<div>
  <div *ngFor="let workflow of getFilteredMyWorkflows(); let i = index" class="workflow-item"
    [class.active]="workflow.id === activeWorkflowId" (click)="redirectToWorkflow(workflow.id)">
    <div class="workflow-info">
      <div class="workflow-name">{{ workflow.name }}</div>
      <div class="workflow-dates">
        <div class="workflow-date">
          <span class="workflow-date-label">Created:</span>
          <span class="date-value">{{ formatDate(workflow.createdAt) }}</span>
        </div>
        <div class="workflow-date">
          <span class="workflow-date-label">Updated:</span>
          <span class="date-value">{{ formatDate(workflow.updatedAt) }}</span>
        </div>
      </div>
    </div>
    <div class="workflow-actions">
      <button class="run-workflow-btn" (click)="runWorkflow(workflow.id, $event)" title="Run Workflow"
        [disabled]="isWorkflowRunning(workflow.id)">
        <svg viewBox="0 0 24 24" fill="currentColor">
          <polygon points="5 3 19 12 5 21 5 3"></polygon>
        </svg>
        <span class="btn-text">Run</span>
      </button>
      <button class="redirect-workflow-btn" (click)="redirectToWorkflow(workflow.id); $event.stopPropagation()" 
        title="Go to Workflow">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
          stroke-linejoin="round">
          <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
          <polyline points="15 3 21 3 21 9"></polyline>
          <line x1="10" y1="14" x2="21" y2="3"></line>
        </svg>
        <span class="btn-text">Direct</span>
      </button>
      <button class="share-workflow-btn" (click)="shareWorkflow(workflow.id, $event)" title="Share Workflow">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
          stroke-linejoin="round">
          <circle cx="18" cy="5" r="3"></circle>
          <circle cx="6" cy="12" r="3"></circle>
          <circle cx="18" cy="19" r="3"></circle>
          <line x1="8.59" y1="13.51" x2="15.42" y2="17.49"></line>
          <line x1="15.41" y1="6.51" x2="8.59" y2="10.49"></line>
        </svg>
        <span class="btn-text">Share</span>
      </button>
    </div>
  </div>
</div>








.workflow-actions {
    display: flex;
    gap: 4px;
    align-items: center;
}

/* Run Workflow Button */
.run-workflow-btn {
    padding: 4px 8px;
    background: var(--agentic-ai-workflow-bg-run-btn);
    border: none;
    border-radius: 4px;
    display: flex;
    align-items: center;
    gap: 4px;
    cursor: pointer;
    transition: all 0.2s;
    flex-shrink: 0;
}

.run-workflow-btn:hover:not(:disabled) {
    background: var(--agentic-ai-workflow-bg-run-btn-hover);
    transform: translateY(-1px);
    box-shadow: 0 2px 4px var(--agentic-ai-workflow-shadow-run-btn);
}

.run-workflow-btn:active:not(:disabled) {
    transform: translateY(0);
}

.run-workflow-btn:disabled {
    opacity: 0.4;
    cursor: not-allowed;
    pointer-events: none;
}

.run-workflow-btn svg {
    width: 12px;
    height: 12px;
    fill: var(--agentic-ai-workflow-text-white);
    stroke: none;
}

.run-workflow-btn .btn-text {
    font-size: 11px;
    font-weight: 500;
    color: var(--agentic-ai-workflow-text-white);
}

/* Redirect Workflow Button */
.redirect-workflow-btn {
    padding: 4px 8px;
    background: var(--agentic-ai-workflow-bg-button);
    border: 1px solid var(--agentic-ai-workflow-border-primary);
    border-radius: 4px;
    display: flex;
    align-items: center;
    gap: 4px;
    cursor: pointer;
    transition: all 0.2s;
    flex-shrink: 0;
}

.redirect-workflow-btn:hover {
    background: var(--agentic-ai-workflow-bg-button-hover);
    border-color: var(--agentic-ai-workflow-border-hover);
    transform: translateY(-1px);
}

.redirect-workflow-btn:active {
    transform: translateY(0);
}

.redirect-workflow-btn svg {
    width: 12px;
    height: 12px;
    stroke: var(--agentic-ai-workflow-text-secondary);
}

.redirect-workflow-btn .btn-text {
    font-size: 11px;
    font-weight: 500;
    color: var(--agentic-ai-workflow-text-secondary);
}

/* Share Workflow Button */
.share-workflow-btn {
    padding: 4px 8px;
    background: var(--agentic-ai-workflow-bg-button);
    border: 1px solid var(--agentic-ai-workflow-border-primary);
    border-radius: 4px;
    display: flex;
    align-items: center;
    gap: 4px;
    cursor: pointer;
    transition: all 0.2s;
    flex-shrink: 0;
}

.share-workflow-btn:hover {
    background: var(--agentic-ai-workflow-bg-button-hover);
    border-color: var(--agentic-ai-workflow-border-hover);
    transform: translateY(-1px);
}

.share-workflow-btn:active {
    transform: translateY(0);
}

.share-workflow-btn svg {
    width: 12px;
    height: 12px;
    stroke: var(--agentic-ai-workflow-text-secondary);
}

.share-workflow-btn .btn-text {
    font-size: 11px;
    font-weight: 500;
    color: var(--agentic-ai-workflow-text-secondary);
}
