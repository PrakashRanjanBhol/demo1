<div class="sidebar">
  <div class="empty-state">
    <span class="emoji">👆</span>
    
    <h2 class="title">No CRS Selected</h2>
    
    <p class="description">
      Select a CRS ticket from the above CRS list to view its associated SRS
    </p>
  </div>
</div>






/* Dark Mode Variables (Default) */
:root {
  --sdlc--suite-selection-bg-primary: #111113;
  --sdlc--suite-selection-text-primary: #e4e4e7;
  --sdlc--suite-selection-text-secondary: #71717a;
}

/* Light Mode Variables */
:root.light-mode {
  --sdlc--suite-selection-bg-primary: #ffffff;
  --sdlc--suite-selection-text-primary: #18181b;
  --sdlc--suite-selection-text-secondary: #71717a;
}

/* Alternatively, you can use a data attribute or class on body/html */
[data-theme="light"] {
  --sdlc--suite-selection-bg-primary: #ffffff;
  --sdlc--suite-selection-text-primary: #18181b;
  --sdlc--suite-selection-text-secondary: #71717a;
}

.sidebar {
  width: 320px;
  background-color: var(--sdlc--suite-selection-bg-primary);
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
}

.empty-state {
  text-align: center;
  animation: fadeIn 0.6s ease-in;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.emoji {
  font-size: 48px;
  margin-bottom: 20px;
  display: block;
  animation: bounceUp 2s ease-in-out infinite;
}

@keyframes bounceUp {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-12px);
  }
}

.title {
  font-size: 18px;
  font-weight: 600;
  color: var(--sdlc--suite-selection-text-primary);
  margin-bottom: 12px;
  letter-spacing: -0.01em;
}

.description {
  font-size: 14px;
  color: var(--sdlc--suite-selection-text-secondary);
  line-height: 1.6;
  margin-bottom: 24px;
}
