/* ============================================================================
   PENDING APPROVAL HIGHLIGHT - Professional & Elegant Animation
   ============================================================================ */

/* Pending approval item in queue - subtle pulse animation */
.vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval {
  background: var(--vibe-bg-secondary);
  border: 1px solid var(--vibe-accent-blue);
  position: relative;
  overflow: hidden;
  animation: vibe-coding-subtle-pulse 2s ease-in-out infinite;
}

/* Elegant glow effect */
.vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(59, 130, 246, 0.1),
    transparent
  );
  animation: vibe-coding-shimmer 3s ease-in-out infinite;
}

/* Dark mode shimmer */
[data-theme="dark"] .vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval::before {
  background: linear-gradient(
    90deg,
    transparent,
    rgba(96, 165, 250, 0.08),
    transparent
  );
}

/* Subtle border pulse */
.vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval::after {
  content: '';
  position: absolute;
  top: -1px;
  left: -1px;
  right: -1px;
  bottom: -1px;
  border: 2px solid var(--vibe-accent-blue);
  border-radius: 6px;
  opacity: 0;
  animation: vibe-coding-border-pulse 2s ease-in-out infinite;
  pointer-events: none;
}

/* Left accent bar */
.vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval .vibe-coding-approval-indicator {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 3px;
  background: var(--vibe-accent-blue);
  animation: vibe-coding-indicator-glow 2s ease-in-out infinite;
}

/* Icon highlight for pending approval */
.vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval .vibe-coding-file-icon {
  background: var(--vibe-accent-blue-light);
  color: var(--vibe-accent-blue-text);
  border-color: var(--vibe-accent-blue);
}

/* Status badge highlight */
.vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval .vibe-coding-status-badge {
  background: var(--vibe-accent-blue);
  color: white;
  border-color: var(--vibe-accent-blue);
  font-weight: 600;
}

/* Subtle pulse animation */
@keyframes vibe-coding-subtle-pulse {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(59, 130, 246, 0);
  }
  50% {
    box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
  }
}

/* Shimmer effect */
@keyframes vibe-coding-shimmer {
  0% {
    left: -100%;
  }
  50%, 100% {
    left: 100%;
  }
}

/* Border pulse animation */
@keyframes vibe-coding-border-pulse {
  0%, 100% {
    opacity: 0;
    transform: scale(1);
  }
  50% {
    opacity: 0.3;
    transform: scale(1.02);
  }
}

/* Indicator glow */
@keyframes vibe-coding-indicator-glow {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(59, 130, 246, 0.4);
  }
  50% {
    box-shadow: 0 0 8px 2px rgba(59, 130, 246, 0.6);
  }
}

/* Dark mode adjustments */
[data-theme="dark"] .vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval {
  border-color: var(--vibe-accent-blue);
}

@media (prefers-reduced-motion: reduce) {
  .vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval,
  .vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval::before,
  .vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval::after,
  .vibe-coding-queued .vibe-coding-file-item.vibe-coding-pending-approval .vibe-coding-approval-indicator {
    animation: none;
  }
}

/* ============================================================================
   END OF PENDING APPROVAL HIGHLIGHT
   ============================================================================ */









<!-- In the Queued Items section, update File Operation Item -->
<ng-container *ngIf="item.tool !== 'READ' && hasFileDetails(item.content)">
  <div class="vibe-coding-file-item" [ngClass]="{'vibe-coding-pending-approval': item.tool === 'WRITE' || item.tool === 'MODIFY'}">
    <!-- Add approval indicator for WRITE/MODIFY -->
    <span *ngIf="item.tool === 'WRITE' || item.tool === 'MODIFY'" class="vibe-coding-approval-indicator"></span>
    
    <div class="vibe-coding-file-icon">{{ getFileExtension(getFileDetails(item.content)?.file_name) }}</div>
    <div class="vibe-coding-file-info">
      <div class="vibe-coding-file-name">{{ getFileDetails(item.content)?.file_name }}</div>
      <div class="vibe-coding-file-path">{{ getFileDetails(item.content)?.file_path }}</div>
    </div>
    <div class="vibe-coding-status-badge">{{ item.tool }}</div>
  </div>
</ng-container>
