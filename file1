<main>
  <div class="content-layout">
    <!-- Left Section: Main Content -->
    <div class="main-content">
      <div class="content-card">
        <h1 class="content-title">Sample Content</h1>
        <p class="content-text">
          This is your main content area. You can add any components, widgets, or content here. 
          The layout features a professional design with a sticky header, flexible content area, 
          and a footer that stays at the bottom. Toggle between light and dark modes using the 
          theme button in the header.
        </p>
        <p class="content-text" style="margin-top: 16px;">
          The right sidebar is collapsible and will adjust its width based on screen size:
          320px for screens less than 1600px, and 380px for larger screens.
        </p>
      </div>
    </div>

    <!-- Separator Line -->
    <div class="separator" [class.hidden]="!isSidebarOpen"></div>

    <!-- Right Section: Sidebar -->
    <aside class="sidebar" [class.collapsed]="!isSidebarOpen">
      <!-- Sidebar Toggle Button -->
      <button class="sidebar-toggle" (click)="toggleSidebar()" [class.collapsed]="!isSidebarOpen" aria-label="Toggle sidebar">
        <svg *ngIf="isSidebarOpen" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5" />
        </svg>
        <svg *ngIf="!isSidebarOpen" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
        </svg>
      </button>

      <div class="sidebar-content">
        <div class="sidebar-header">
          <h2 class="sidebar-title">Sidebar Content</h2>
        </div>
        <div class="sidebar-body">
          <p class="sidebar-text">
            This is the right sidebar section. You can add additional content, widgets, 
            or tools here. The sidebar can be collapsed and expanded using the toggle button.
          </p>
        </div>
      </div>
    </aside>
  </div>
</main>








/* Main Content Styles */
:host {
  display: flex;
  flex-direction: column;
  flex: 1;
}

main {
  flex: 1;
  max-width: 1400px;
  width: 100%;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  overflow-x: hidden;
}

.content-layout {
  display: flex;
  gap: 0;
  flex: 1;
  position: relative;
  overflow-x: hidden;
}

/* Main Content Section */
.main-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.content-card {
  background-color: var(--header-bg);
  border-radius: 12px;
  padding: 32px;
  transition: background-color 0.3s ease;
  flex: 1;
}

.content-title {
  font-size: 20px;
  font-weight: 600;
  margin-bottom: 16px;
  color: var(--text-color);
}

.content-text {
  font-size: 14px;
  line-height: 1.6;
  color: var(--text-color);
  opacity: 0.8;
}

/* Separator Line */
.separator {
  width: 1px;
  background-color: var(--border-color);
  transition: background-color 0.3s ease, opacity 0.3s ease, width 0.3s ease;
  flex-shrink: 0;
}

.separator.hidden {
  opacity: 0;
  width: 0;
}

/* Sidebar Section */
.sidebar {
  width: 320px;
  background-color: var(--header-bg);
  border-radius: 12px;
  transition: width 0.3s ease, opacity 0.3s ease, background-color 0.3s ease;
  flex-shrink: 0;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  position: relative;
}

.sidebar.collapsed {
  width: 0;
  opacity: 0;
}

.sidebar-content {
  height: 100%;
  display: flex;
  flex-direction: column;
  min-width: 320px;
}

.sidebar-header {
  padding: 24px 24px 16px 24px;
  border-bottom: 1px solid var(--border-color);
  flex-shrink: 0;
}

.sidebar-title {
  font-size: 18px;
  font-weight: 600;
  color: var(--text-color);
  margin: 0;
}

.sidebar-body {
  flex: 1;
  overflow-y: auto;
  padding: 24px;
}

.sidebar-text {
  font-size: 14px;
  line-height: 1.6;
  color: var(--text-color);
  opacity: 0.8;
}

/* Sidebar Toggle Button */
.sidebar-toggle {
  position: absolute;
  left: -16px;
  top: 50%;
  transform: translateY(-50%);
  width: 32px;
  height: 80px;
  background-color: var(--header-bg);
  border: 1px solid var(--border-color);
  border-radius: 8px 0 0 8px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
  z-index: 10;
  box-shadow: -2px 0 8px rgba(0, 0, 0, 0.05);
}

.sidebar-toggle:hover {
  background-color: var(--footer-bg);
  left: -18px;
  box-shadow: -3px 0 12px rgba(0, 0, 0, 0.1);
}

.sidebar-toggle.collapsed {
  position: fixed;
  right: 0;
  left: auto;
  border-radius: 8px 0 0 8px;
  box-shadow: -2px 0 8px rgba(0, 0, 0, 0.1);
}

.sidebar-toggle.collapsed:hover {
  right: 2px;
  left: auto;
}

.sidebar-toggle svg {
  width: 18px;
  height: 18px;
  stroke: var(--text-color);
  stroke-width: 2;
  transition: transform 0.2s ease;
}

.sidebar-toggle:hover svg {
  transform: scale(1.15);
}

@media (max-width: 768px) {
  .sidebar-toggle {
    right: 12px;
    bottom: 12px;
    width: 36px;
    height: 36px;
  }
  
  .sidebar-toggle svg {
    width: 16px;
    height: 16px;
  }
}

/* Responsive breakpoint for larger screens */
@media (min-width: 1600px) {
  .sidebar {
    width: 380px;
  }

  .sidebar-content {
    min-width: 380px;
  }
}

/* Responsive Design for tablets and mobile */
@media (max-width: 1024px) {
  .content-layout {
    flex-direction: column;
  }

  .main-content {
    padding-right: 0;
    padding-bottom: 20px;
  }

  .separator {
    width: 100%;
    height: 1px;
    margin: 20px 0;
  }

  .separator.hidden {
    height: 0;
    margin: 0;
  }

  .sidebar {
    width: 100%;
    max-height: 400px;
  }

  .sidebar.collapsed {
    max-height: 0;
    width: 100%;
  }

  .sidebar-content {
    min-width: 100%;
  }
}

@media (max-width: 768px) {
  .content-card {
    padding: 24px;
  }

  .sidebar-header {
    padding: 20px 20px 16px 20px;
  }

  .sidebar-body {
    padding: 20px;
  }

  .content-layout {
    gap: 0;
  }

  .sidebar-toggle {
    position: fixed;
    left: auto;
    right: 16px;
    bottom: 16px;
    top: auto;
    transform: none;
    width: 40px;
    height: 40px;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .sidebar-toggle:hover {
    left: auto;
    right: 16px;
    bottom: 14px;
  }

  .sidebar-toggle.collapsed {
    right: 16px;
    left: auto;
    border-radius: 8px;
  }

  .sidebar-toggle.collapsed:hover {
    right: 16px;
    bottom: 14px;
  }

  .sidebar-toggle svg {
    width: 20px;
    height: 20px;
  }
}
