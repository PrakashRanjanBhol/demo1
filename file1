/* Add specific classes for ADK and Artifacts sections */
.accordion-section.adk-section .accordion-header {
    border-left: 3px solid transparent;
    transition: all 0.2s;
}

.accordion-section.adk-section .accordion-header:hover {
    background: rgba(102, 126, 234, 0.05);
    border-left-color: #667eea;
}

.accordion-section.artifacts-section .accordion-header {
    border-left: 3px solid transparent;
    transition: all 0.2s;
}

.accordion-section.artifacts-section .accordion-header:hover {
    background: rgba(34, 197, 94, 0.05);
    border-left-color: #22c55e;
}

/* Update base .accordion-header to remove conflicting styles: */
.accordion-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 12px 20px;
    background: var(--adk-agents-accordion-header-bg);
    cursor: pointer;
    transition: all 0.2s;
    border-top: 1px solid var(--adk-agents-border);
    border-bottom: 1px solid var(--adk-agents-border);
    border-left: 3px solid transparent;
}

.accordion-header:hover {
    /* Remove this - will be handled by specific classes */
}






<!-- Update the accordion sections with specific classes: -->

<!-- ADK Accordion -->
<div class="accordion-section adk-section">
  <div class="accordion-header" (click)="toggleAdkTab()">
    <span class="accordion-title">ADK</span>
    <svg class="chevron-icon" [class.expanded]="isAdkExpanded" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
    </svg>
  </div>
  <!-- rest of the content -->
</div>

<!-- Artifacts Accordion -->
<div class="accordion-section artifacts-section">
  <div class="accordion-header" (click)="toggleArtifactsTab()">
    <div class="accordion-title-container">
      <span class="accordion-title">Artifacts</span>
      <span class="accordion-subtitle" *ngIf="selectedAgent">{{ selectedAgent }}</span>
    </div>
    <svg class="chevron-icon" [class.expanded]="isArtifactsExpanded" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
    </svg>
  </div>
  <!-- rest of the content -->
</div>
