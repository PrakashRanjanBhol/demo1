/* Reduce row and cell padding */
.dataTable :global(.p-datatable .p-datatable-tbody > tr > td),
.dataTable :global(.p-datatable .p-datatable-thead > tr > th) {
  padding: 0.5rem 0.75rem;
  font-size: 0.78rem;
  line-height: 1.2;
}

/* Compact paginator */
.dataTable :global(.p-paginator) {
  padding: 0.25rem 0.5rem;
  font-size: 0.75rem;
}

/* Prevent unexpected margins */
.dataTable {
  margin: 0;
  padding: 0;
  overflow: hidden;
}

/* Optional: restrict container height */
.dataTableWrapper {
  max-height: calc(100vh - 150px); /* adjust based on your layout */
  overflow-y: auto;
}



<div className={styles.dataTableWrapper}>
  <DataTable
    value={data}
    scrollable
    scrollHeight="flex"
    className={styles.dataTable}
    paginator
    rows={10}
    resizableColumns
    columnResizeMode="fit"
  >
    {/* columns here */}
  </DataTable>
</div>
