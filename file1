hr {
  border: none;
  height: 2px;
  margin: 2rem 0;
  background: linear-gradient(
    to right,
    transparent,
    currentColor,
    transparent
  );
  opacity: 0.3;
}

/* Dark mode */
@media (prefers-color-scheme: dark) {
  hr {
    opacity: 0.2;
    background: linear-gradient(
      to right,
      transparent,
      #4a4a4f,
      transparent
    );
  }
}

/* Light mode */
@media (prefers-color-scheme: light) {
  hr {
    opacity: 0.3;
    background: linear-gradient(
      to right,
      transparent,
      #d1d1d6,
      transparent
    );
  }
}
