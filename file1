const handleCloseModal = () => {
    setIsModalOpen(false);
    setActiveView('upload');
    setUrlInput('');
    setErrorMessage(null);
};


<div className={styles.uploadModalOverlay} onClick={handleCloseModal}>
    <div className={styles.uploadModal} onClick={(e) => e.stopPropagation()}>

        <div className={styles.modalHeader}>
            <div className={`${styles.headerText} ${headerFading ? styles.headerFading : ''}`}>
                <span className={styles.headerLine1}>
                    {HEADER_MESSAGES[headerIndex].line1}
                </span>
                <span className={styles.headerLine2}>
                    {HEADER_MESSAGES[headerIndex].line2}
                </span>
            </div>
            <button className={styles.closeModalBtn} onClick={handleCloseModal}>
                <IconX size={18} />
            </button>
        </div>

        ...

    </div>
</div>



// in handleUrlInsert
const handleUrlInsert = () => {
    const trimmedUrl = urlInput.trim();
    if (!trimmedUrl) return;
    
    // ... existing logic ...

    handleCloseModal();
}

// in processFiles
const processFiles = (files: FileList | File[]) => {
    
    // ... existing logic ...

    handleCloseModal();
}
