<div class="container">
    <h2 class="header-title">📊 Lines of Code Overview</h2>

    <div class="filters">
        <input type="date" [(ngModel)]="fromDate" [max]="toDate" (change)="onDateChange()" />
        <input type="date" [(ngModel)]="toDate" [max]="todayStr" (change)="onDateChange()" />
    </div>

    <div class="pagination-controls">
        <button (click)="prevPage()" [disabled]="pageIndex === 0">⬅️</button>
        <span>Page {{ pageIndex + 1 }} / {{ totalPages }}</span>
        <button (click)="nextPage()" [disabled]="pageIndex >= totalPages - 1">➡️</button>
    </div>

    <div #chartContainer class="chart-container"></div>
</div>
