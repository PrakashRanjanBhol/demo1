/* Responsive Design for smaller screens */
@media screen and (max-width: 1400px) {
    /* Reduce panel padding */
    .panel-header {
        padding: 16px;
    }

    .panel-content {
        padding: 16px;
        gap: 12px;
    }

    /* Smaller panel title */
    .panel-title {
        font-size: 14px;
        padding-left: 12px;
    }

    .panel-title::before {
        width: 2px;
        height: 16px;
    }

    /* Smaller date range */
    .date-range {
        gap: 6px;
        padding: 4px 8px;
    }

    .date-icon {
        width: 14px;
        height: 14px;
    }

    .arrow-icon {
        width: 12px;
        height: 12px;
    }

    .date-text {
        font-size: 10px;
    }

    /* Smaller section headers */
    .content-section-header {
        gap: 8px;
        padding: 10px 12px;
    }

    .content-header-icon {
        width: 24px;
        height: 24px;
    }

    .content-header-icon svg {
        width: 16px;
        height: 16px;
    }

    .content-header-title {
        font-size: 12px;
    }

    .content-count {
        font-size: 10px;
        padding: 3px 8px;
    }

    .collapse-toggle-btn {
        width: 20px;
        height: 20px;
    }

    .collapse-toggle-btn svg {
        width: 16px;
        height: 16px;
    }

    /* Smaller history items */
    .history-content {
        gap: 8px;
    }

    .history-item {
        gap: 10px;
        padding: 10px 12px;
        border-radius: 8px;
    }

    .history-item-icon {
        width: 32px;
        height: 32px;
        border-radius: 8px;
    }

    .history-item-icon svg {
        width: 16px;
        height: 16px;
    }

    .history-item-name {
        font-size: 12px;
        margin: 0 0 3px 0;
    }

    .history-item-date {
        font-size: 11px;
    }

    .history-item-checkmark {
        width: 28px;
        height: 28px;
    }

    .history-item-checkmark svg {
        width: 16px;
        height: 16px;
    }

    .history-item-action {
        width: 28px;
        height: 28px;
        border-radius: 6px;
    }

    .history-item-action svg {
        width: 14px;
        height: 14px;
    }

    /* Smaller no results message */
    .no-results {
        padding: 36px 20px;
    }

    .no-results svg {
        width: 40px;
        height: 40px;
        margin-bottom: 12px;
    }

    .no-results p {
        font-size: 13px;
    }
}
