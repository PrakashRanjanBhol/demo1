// Updated clearDateRange method - Replace your existing clearDateRange method

export class PrSidebarComponent implements OnInit {
  // ... existing properties and methods ...

  clearDateRange(): void {
    // Clear the input fields
    this.startDate = '';
    this.endDate = '';
    
    // Reset to initial date range (last 3 months) instead of clearing completely
    this.initializeDateRange();
    
    // Note: This will reset both the inputs and applied dates to the initial 3-month range
    // The repo info section will continue to show the date range
  }

  // Alternative: If you want to clear inputs but keep applied dates until user clicks Apply
  /*
  clearDateRange(): void {
    // Clear only the input fields
    this.startDate = '';
    this.endDate = '';
    
    // Don't clear appliedStartDate and appliedEndDate
    // This keeps the filter active and the date period visible in repo info
    // User needs to click Apply to update the filter
  }
  */

  // Alternative 2: If you want to clear completely but show a "Clear All Filters" button instead
  /*
  clearDateRange(): void {
    // Clear the input fields
    this.startDate = '';
    this.endDate = '';
    // Don't clear applied dates here
  }

  clearAllFilters(): void {
    // Clear everything including applied dates
    this.startDate = '';
    this.endDate = '';
    this.appliedStartDate = '';
    this.appliedEndDate = '';
    this.updatePRList();
  }
  */

  // ... rest of the component ...
}
