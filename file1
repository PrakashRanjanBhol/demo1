/* Multiselect Panel (the dropdown) */
.dataTable :global(.p-multiselect-panel) {
  background-color: #222 !important;
  border: 1px solid #444;
  color: #eee;
  font-size: 0.8rem;
}

/* Filter input inside MultiSelect */
.dataTable :global(.p-multiselect-filter-container .p-inputtext) {
  background-color: #2c2c2c !important;
  border: 1px solid #555;
  color: #e0e0e0;
  font-size: 0.8rem;
  padding: 0.35rem 0.5rem;
}

/* MultiSelect options */
.dataTable :global(.p-multiselect-items .p-multiselect-item) {
  padding: 0.4rem 0.5rem;
  font-size: 0.8rem;
  color: #e0e0e0;
}

/* Selected/hovered items */
.dataTable :global(.p-multiselect-items .p-multiselect-item.p-highlight) {
  background-color: #444 !important;
  color: #fff;
}

.dataTable :global(.p-multiselect-items .p-multiselect-item:hover) {
  background-color: #333 !important;
  color: #fff;
}

/* Checkbox inside multiselect */
.dataTable :global(.p-multiselect .p-checkbox-box) {
  background-color: #2a2a2a !important;
  border: 1px solid #555;
  width: 1rem;
  height: 1rem;
}

/* Selected checkbox icon */
.dataTable :global(.p-checkbox .p-checkbox-icon) {
  color: #00bcd4;
  font-size: 0.7rem;
}

/* Clear/Select all button at bottom (if any) */
.dataTable :global(.p-multiselect-footer .p-button) {
  background-color: #2c2c2c;
  border: 1px solid #555;
  color: #eee;
  font-size: 0.75rem;
  padding: 0.4rem 0.75rem;
}
