enforceLimit(event: any) {
  let value = event.target.value;

  // allow empty while typing
  if (value === '') return;

  let num = Number(value);

  if (num < 1) {
    event.target.value = 1;
  } else if (num > 1000) {
    event.target.value = 1000;
  }
}
