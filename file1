<div class="vibe-coding-files-list">
  <div *ngFor="let file of getModifiedFiles()" class="vibe-coding-modified-file">
    <!-- MODIFIED: New layout structure -->
    <div class="vibe-coding-modified-file-name-row">
      <span class="vibe-coding-modified-filename">{{ file.fileName }}</span>
      <span *ngIf="file.isNewFile && !file.rejected" class="vibe-coding-new-file-badge">New File</span>
    </div>
    
    <div *ngIf="!file.isNewFile" class="vibe-coding-modified-filepath">{{ file.filePath }}</div>
    
    <!-- MODIFIED: Tool and Status in one row -->
    <div class="vibe-coding-modified-file-badges">
      <span class="vibe-coding-modified-badge" [ngClass]="{'vibe-coding-badge-write': file.tool === 'WRITE', 'vibe-coding-badge-modify': file.tool === 'MODIFY'}">
        {{ file.tool }}
      </span>
      <span 
        class="vibe-coding-status-indicator" 
        [ngClass]="{'vibe-coding-indicator-rejected': file.rejected, 'vibe-coding-indicator-accepted': !file.rejected}">
        {{ file.rejected ? 'REJECTED' : 'ACCEPTED' }}
      </span>
    </div>
  </div>
</div>









/* MODIFIED: Files list layout */
.vibe-coding-modified-file {
  padding: 12px 14px;
  border-bottom: 1px solid var(--vibe-border-primary);
  background: var(--vibe-bg-primary);
  transition: background 0.2s ease;
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.vibe-coding-modified-file:last-child {
  border-bottom: none;
}

.vibe-coding-modified-file:hover {
  background: var(--vibe-bg-hover);
}

/* MODIFIED: Filename row */
.vibe-coding-modified-file-name-row {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

/* REMOVED: .vibe-coding-modified-file-header - no longer needed */

.vibe-coding-modified-filename {
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--vibe-text-primary);
  word-break: break-word;
  flex: 1;
  min-width: 0;
}

.vibe-coding-modified-filepath {
  font-size: 0.75rem;
  color: var(--vibe-text-tertiary);
  word-break: break-word;
  font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
}

/* NEW: Badges row - Tool and Status together */
.vibe-coding-modified-file-badges {
  display: flex;
  gap: 6px;
  align-items: center;
  flex-wrap: wrap;
}

.vibe-coding-modified-badge {
  font-size: 0.65rem;
  font-weight: 600;
  padding: 3px 8px;
  border-radius: 3px;
  text-transform: uppercase;
  letter-spacing: 0.3px;
  flex-shrink: 0;
}

.vibe-coding-modified-badge.vibe-coding-badge-write {
  background: var(--vibe-badge-write-bg);
  color: var(--vibe-badge-write-text);
}

.vibe-coding-modified-badge.vibe-coding-badge-modify {
  background: var(--vibe-badge-modify-bg);
  color: var(--vibe-badge-modify-text);
}

/* Status Indicator - Updated sizing */
.vibe-coding-status-indicator {
  font-size: 0.65rem;
  font-weight: 600;
  padding: 3px 8px;
  border-radius: 3px;
  text-transform: uppercase;
  letter-spacing: 0.3px;
  flex-shrink: 0;
}

.vibe-coding-status-indicator.vibe-coding-indicator-accepted {
  background: var(--vibe-status-accepted-bg);
  color: var(--vibe-status-accepted-text);
  border: 1px solid var(--vibe-status-accepted-border);
}

.vibe-coding-status-indicator.vibe-coding-indicator-rejected {
  background: var(--vibe-status-rejected-bg);
  color: var(--vibe-status-rejected-text);
  border: 1px solid var(--vibe-status-rejected-border);
}

/* New File Badge - Updated */
.vibe-coding-new-file-badge {
  font-size: 0.65rem;
  font-weight: 600;
  padding: 3px 8px;
  border-radius: 3px;
  background: var(--vibe-accent-green);
  color: white;
  text-transform: uppercase;
  letter-spacing: 0.3px;
  flex-shrink: 0;
}
