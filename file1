connection: string | undefined;

  constructor(private router: Router) {
    const nav = this.router.getCurrentNavigation();
    this.connection = nav?.extras?.state?.['connection'];
  }


  this.router.navigate(['/dashboard'], {
      state: { connection: 'SSO' }
    });
