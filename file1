/* Update .artifact-item: */
.artifact-item {
    cursor: pointer;
    padding: 8px 14px;
    margin-bottom: 4px;
    border-radius: 0;
    border: none;
    background: transparent;
}

.artifact-item:hover {
    background: transparent;
    transform: none;
}

.artifact-item.active {
    background: transparent;
}
