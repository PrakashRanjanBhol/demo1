// Update FileInfo interface
interface FileInfo {
  id: number;
  name: string;
  path: string;
  size: string;
  repository: string;
  content: string;
  type: string;
  projectName: string; // Add this
}

// Update files array - add projectName to each file
files: FileInfo[] = [
  {
    id: 1,
    name: 'App.jsx',
    path: 'src/',
    size: '2.4 KB',
    repository: 'frontend-app',
    projectName: 'MyProject', // Add this
    type: 'javascript',
    content: `...` // existing content
  },
  {
    id: 2,
    name: 'index.css',
    path: 'src/',
    size: '1.2 KB',
    repository: 'frontend-app',
    projectName: 'MyProject', // Add this
    type: 'css',
    content: `...` // existing content
  },
  {
    id: 3,
    name: 'utils.js',
    path: 'src/lib/',
    size: '3.8 KB',
    repository: 'frontend-app',
    projectName: 'MyProject', // Add this
    type: 'javascript',
    content: `...` // existing content
  },
  {
    id: 4,
    name: 'README.md',
    path: '/',
    size: '5.1 KB',
    repository: 'backend-api',
    projectName: 'Backend Services', // Add this
    type: 'markdown',
    content: `...` // existing content
  }
];














<!-- File Header -->
<ng-container *ngIf="activeView === 'file' && selectedFile">
  <div class="breadcrumb animate-in">
    <svg class="breadcrumb-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"/>
    </svg>
    <span class="breadcrumb-text">{{ selectedFile.projectName }}</span>
    <svg class="breadcrumb-separator" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 18 15 12 9 6"/>
    </svg>
    <span class="breadcrumb-text">{{ selectedFile.repository }}</span>
    <svg class="breadcrumb-separator" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 18 15 12 9 6"/>
    </svg>
    <span class="breadcrumb-text">File</span>
    <svg class="breadcrumb-separator" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 18 15 12 9 6"/>
    </svg>
    <span class="breadcrumb-text breadcrumb-filename">{{ selectedFile.name }}</span>
  </div>
  <div class="main-heading animate-in">
    <!-- ... rest remains same ... -->
  </div>
  <div class="sub-info animate-in" [class.collapsed]="!isPRHeaderExpanded">
    <!-- ... rest remains same ... -->
  </div>
</ng-container>













/* Breadcrumb text styling */
.breadcrumb-text {
  display: inline-block;
  max-width: 200px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  vertical-align: bottom;
}

/* Filename in breadcrumb - allow more width */
.breadcrumb-filename {
  max-width: 300px;
  font-weight: 500;
}

/* Responsive breadcrumb */
@media (max-width: 768px) {
  .breadcrumb-text {
    max-width: 100px;
  }
  
  .breadcrumb-filename {
    max-width: 150px;
  }
}
