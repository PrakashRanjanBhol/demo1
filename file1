{
  "settings": {
    "title": "Settings",
    "subtitle": "Manage your workspace",
    "nav": {
      "personalInformation": "Personal Information",
      "roleManagement": "Role Management",
      "ragManagement": "RAG Management",
      "generalSettings": "General Settings"
    },
    "roleManagement": {
      "title": "Role Management",
      "description": "Manage roles and permissions across your organization.",
      "table": {
        "user": "User",
        "email": "Email",
        "accessRole": "Access Role",
        "actions": "Actions"
      },
      "roles": {
        "admin": "Admin",
        "user": "User"
      },
      "actions": {
        "edit": "Edit",
        "save": "Save",
        "cancel": "Cancel"
      },
      "status": {
        "loading": "Loading users...",
        "error": "Something went wrong. Please try again.",
        "retry": "Retry",
        "noResults": "No results found",
        "records": "record",
        "recordsPlural": "records"
      }
    }
  }
}











{
  "settings": {
    "title": "설정",
    "subtitle": "워크스페이스 관리",
    "nav": {
      "personalInformation": "개인 정보",
      "roleManagement": "역할 관리",
      "ragManagement": "RAG 관리",
      "generalSettings": "일반 설정"
    },
    "roleManagement": {
      "title": "역할 관리",
      "description": "조직의 역할 및 권한을 관리합니다.",
      "table": {
        "user": "사용자",
        "email": "이메일",
        "accessRole": "접근 역할",
        "actions": "작업"
      },
      "roles": {
        "admin": "관리자",
        "user": "사용자"
      },
      "actions": {
        "edit": "편집",
        "save": "저장",
        "cancel": "취소"
      },
      "status": {
        "loading": "사용자 불러오는 중...",
        "error": "오류가 발생했습니다. 다시 시도해주세요.",
        "retry": "재시도",
        "noResults": "결과를 찾을 수 없습니다",
        "records": "건",
        "recordsPlural": "건"
      }
    }
  }
}









import { useTranslation } from 'react-i18next';
import { NavLink, Outlet } from 'react-router-dom';
import styles from './SettingsLayout.module.scss';

export default function SettingsLayout() {
  const { t } = useTranslation();

  const tabs = [
    { label: t('settings.nav.personalInformation'), path: 'personal-information' },
    { label: t('settings.nav.roleManagement'), path: 'role-management' },
    { label: t('settings.nav.ragManagement'), path: 'rag-management' },
    { label: t('settings.nav.generalSettings'), path: 'general-settings' },
  ];

  const icons = [
    // Personal Information
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
      <circle cx="12" cy="8" r="4" />
      <path d="M4 20c0-4 3.6-7 8-7s8 3 8 7" />
    </svg>,
    // Role Management
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
      <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
      <circle cx="9" cy="7" r="4" />
      <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
      <path d="M16 3.13a4 4 0 0 1 0 7.75" />
    </svg>,
    // RAG Management
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
      <ellipse cx="12" cy="5" rx="9" ry="3" />
      <path d="M3 5v6c0 1.66 4.03 3 9 3s9-1.34 9-3V5" />
      <path d="M3 11v6c0 1.66 4.03 3 9 3s9-1.34 9-3v-6" />
    </svg>,
    // General Settings
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
      <circle cx="12" cy="12" r="3" />
      <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
    </svg>,
  ];

  return (
    <main className={styles.layout}>
      <aside className={styles.sidebar}>

        <div className={styles.sidebarHeader}>
          <div className={styles.headerIcon}>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
              <circle cx="12" cy="12" r="3" />
              <path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z" />
            </svg>
          </div>
          <div className={styles.headerText}>
            <span className={styles.headerTitle}>{t('settings.title')}</span>
            <span className={styles.headerSub}>{t('settings.subtitle')}</span>
          </div>
        </div>

        <span className={styles.sectionLabel}>{t('settings.title')}</span>

        {tabs.map((tab, index) => (
          <NavLink
            key={tab.path}
            to={tab.path}
            className={({ isActive }) =>
              `${styles.navLink} ${isActive ? styles.active : ''}`
            }
          >
            <span className={styles.navIcon}>{icons[index]}</span>
            <span className={styles.navLabel}>{tab.label}</span>
            <span className={styles.activePill} />
          </NavLink>
        ))}

      </aside>

      <section className={styles.content}>
        <Outlet />
      </section>
    </main>
  );
}











import { ColumnDef } from '@tanstack/react-table';
import { useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { useDispatch, useSelector } from 'react-redux';
import { RoleUser } from '../api/roleApi';
import DataTable from '../components/DataTable/DataTable';
import DataTableSkeleton from '../components/DataTable/DataTableSkeleton';
import type { AppDispatch, RootState } from '../store';
import { fetchRoles, updateRole } from '../store/slices/roleSlice';
import styles from './PageShell.module.scss';
import roleStyles from './RoleManagement.module.scss';

export default function RoleManagement() {
  const { t } = useTranslation();
  const dispatch = useDispatch<AppDispatch>();
  const { users, loading, error, updating } = useSelector((state: RootState) => state.roles);

  const [editingId, setEditingId] = useState<number | null>(null);
  const [editingRole, setEditingRole] = useState<'user' | 'admin'>('user');

  useEffect(() => {
    dispatch(fetchRoles());
  }, [dispatch]);

  const handleEdit = (row: RoleUser) => {
    setEditingId(row.ms_user_id);
    setEditingRole(row.role);
  };

  const handleSave = async (ms_user_id: number) => {
    await dispatch(updateRole({ ms_user_id, role: editingRole }));
    setEditingId(null);
  };

  const handleCancel = () => setEditingId(null);

  const columns: ColumnDef<RoleUser, unknown>[] = [
    {
      accessorKey: 'username',
      header: t('settings.roleManagement.table.user'),
      size: 220,
    },
    {
      accessorKey: 'email',
      header: t('settings.roleManagement.table.email'),
      size: 260,
    },
    {
      accessorKey: 'role',
      header: t('settings.roleManagement.table.accessRole'),
      size: 180,
      cell: ({ row }) =>
        editingId === row.original.ms_user_id ? (
          <div className={roleStyles.selectWrapper}>
            <select
              className={roleStyles.roleSelect}
              value={editingRole}
              onChange={(e) => setEditingRole(e.target.value as 'user' | 'admin')}
            >
              <option value="admin">{t('settings.roleManagement.roles.admin')}</option>
              <option value="user">{t('settings.roleManagement.roles.user')}</option>
            </select>
            <span className={roleStyles.selectIcon}>
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
                <polyline points="6 9 12 15 18 9" />
              </svg>
            </span>
          </div>
        ) : (
          <span className={`${roleStyles.roleBadge} ${row.original.role === 'admin' ? roleStyles.admin : roleStyles.user}`}>
            {row.original.role === 'admin'
              ? t('settings.roleManagement.roles.admin')
              : t('settings.roleManagement.roles.user')}
          </span>
        ),
    },
    {
      id: 'actions',
      header: t('settings.roleManagement.table.actions'),
      enableSorting: false,
      size: 120,
      cell: ({ row }) =>
        editingId === row.original.ms_user_id ? (
          <div className={roleStyles.actions}>
            <button
              className={`${roleStyles.actionBtn} ${roleStyles.save}`}
              onClick={() => handleSave(row.original.ms_user_id)}
              disabled={updating === row.original.ms_user_id}
              title={t('settings.roleManagement.actions.save')}
            >
              {updating === row.original.ms_user_id ? (
                <span className={roleStyles.spinner} />
              ) : (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
                  <polyline points="20 6 9 17 4 12" />
                </svg>
              )}
            </button>
            <button
              className={`${roleStyles.actionBtn} ${roleStyles.cancel}`}
              onClick={handleCancel}
              title={t('settings.roleManagement.actions.cancel')}
            >
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
                <line x1="18" y1="6" x2="6" y2="18" />
                <line x1="6" y1="6" x2="18" y2="18" />
              </svg>
            </button>
          </div>
        ) : (
          <div className={roleStyles.actions}>
            <button
              className={`${roleStyles.actionBtn} ${roleStyles.edit}`}
              onClick={() => handleEdit(row.original)}
              title={t('settings.roleManagement.actions.edit')}
            >
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7" />
                <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z" />
              </svg>
            </button>
          </div>
        ),
    },
  ];

  return (
    <div className={styles.page}>
      <div className={styles.pageHeader}>
        <h1 className={styles.title}>{t('settings.roleManagement.title')}</h1>
        <p className={styles.description}>{t('settings.roleManagement.description')}</p>
      </div>
      <div className={styles.pageContent}>
        {loading ? (
          <DataTableSkeleton rows={6} columns={4} />
        ) : error ? (
          <div className={roleStyles.stateBox}>
            <p className={roleStyles.errorText}>{t('settings.roleManagement.status.error')}</p>
            <button className={roleStyles.retryBtn} onClick={() => dispatch(fetchRoles())}>
              {t('settings.roleManagement.status.retry')}
            </button>
          </div>
        ) : (
          <DataTable columns={columns} data={users} pageSize={6} />
        )}
      </div>
    </div>
  );
}
