<div class="sidebar" [class.dark-mode]="isDarkMode">
  <div class="sidebar-header">
    <h2 class="sidebar-title">Documentation</h2>
    <div class="sidebar-actions">
      <!-- View Icon (Eye) -->
      <button 
        class="icon-button" 
        title="View" 
        aria-label="View"
        (click)="handleView()">
        <svg viewBox="0 0 24 24">
          <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
          <circle cx="12" cy="12" r="3"></circle>
        </svg>
      </button>
      
      <!-- Create Icon (Plus) -->
      <button 
        class="icon-button" 
        title="Create" 
        aria-label="Create"
        (click)="handleCreate()">
        <svg viewBox="0 0 24 24">
          <line x1="12" y1="5" x2="12" y2="19"></line>
          <line x1="5" y1="12" x2="19" y2="12"></line>
        </svg>
      </button>
    </div>
  </div>
  
  <div class="sidebar-content">
    <!-- Your sidebar content goes here -->
    <p>Sidebar content area</p>
    
    <!-- Theme toggle button for demo -->
    <button class="theme-toggle" (click)="toggleTheme()">
      Toggle {{ isDarkMode ? 'Light' : 'Dark' }} Mode
    </button>
  </div>
</div>









/* Light Mode Variables (Default) */
.sidebar {
  --code-rag-doc-bg-primary: #ffffff;
  --code-rag-doc-bg-secondary: #f5f5f5;
  --code-rag-doc-border: #e0e0e0;
  --code-rag-doc-text-primary: #333333;
  --code-rag-doc-text-secondary: #666666;
  --code-rag-doc-icon-color: #555555;
  --code-rag-doc-hover-bg: #f0f0f0;
  --code-rag-doc-active-bg: #e0e0e0;
}

/* Dark Mode Variables */
.sidebar.dark-mode {
  --code-rag-doc-bg-primary: #111113;
  --code-rag-doc-bg-secondary: #1a1a1c;
  --code-rag-doc-border: #2a2a2c;
  --code-rag-doc-text-primary: #e0e0e0;
  --code-rag-doc-text-secondary: #a0a0a0;
  --code-rag-doc-icon-color: #b0b0b0;
  --code-rag-doc-hover-bg: #2a2a2c;
  --code-rag-doc-active-bg: #3a3a3c;
}

/* Component Styles */
.sidebar {
  width: 320px;
  background-color: var(--code-rag-doc-bg-primary);
  border: 1px solid var(--code-rag-doc-border);
  border-radius: 8px;
  overflow: hidden;
  transition: background-color 0.3s ease, border-color 0.3s ease;
}

.sidebar-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 20px;
  background-color: var(--code-rag-doc-bg-primary);
  border-bottom: 1px solid var(--code-rag-doc-border);
  transition: background-color 0.3s ease, border-color 0.3s ease;
}

.sidebar-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--code-rag-doc-text-primary);
  margin: 0;
  transition: color 0.3s ease;
}

.sidebar-actions {
  display: flex;
  gap: 12px;
  align-items: center;
}

.icon-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  border: none;
  background-color: transparent;
  cursor: pointer;
  border-radius: 6px;
  transition: background-color 0.2s ease;
  padding: 0;
}

.icon-button:hover {
  background-color: var(--code-rag-doc-hover-bg);
}

.icon-button:active {
  background-color: var(--code-rag-doc-active-bg);
}

.icon-button svg {
  width: 20px;
  height: 20px;
  stroke: var(--code-rag-doc-icon-color);
  fill: none;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
  transition: stroke 0.3s ease;
}

.sidebar-content {
  padding: 20px;
  color: var(--code-rag-doc-text-secondary);
  transition: color 0.3s ease;
}

.sidebar-content p {
  margin: 0 0 16px 0;
}

/* Theme toggle button (for demo purposes) */
.theme-toggle {
  padding: 8px 16px;
  background-color: var(--code-rag-doc-hover-bg);
  color: var(--code-rag-doc-text-primary);
  border: 1px solid var(--code-rag-doc-border);
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  transition: all 0.2s ease;
}

.theme-toggle:hover {
  background-color: var(--code-rag-doc-active-bg);
}
