const options: RequestInit = {
  method: 'POST',  // ✅ Already POST
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer your-token-here'
  },
  body: JSON.stringify({
    query: 'your query here'
  })
};

await this.toolParser.startStreaming(apiUrl, options);
