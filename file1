<div className={styles.alternativeSourceGrid}>
    <button
        className={styles.altSourceBtn}
        onClick={() => { setActiveView('youtube'); setErrorMessage(null); }}>
        <IconYoutube
            size={20}
            className={`${styles.altIcon} ${styles.youtube}`} />
        <span>Youtube URL</span>
    </button>
    <button
        className={styles.altSourceBtn}
        onClick={() => { setActiveView('website'); setErrorMessage(null); }}>
        <IconLink
            size={20}
            className={`${styles.altIcon} ${styles.website}`} />
        <span>Website URL</span>
    </button>
    <button className={styles.altSourceBtn}>
        <IconText
            size={20}
            className={`${styles.altIcon} ${styles.text}`} />
        <span>Text Content</span>
    </button>
</div>



.altIcon {
    &.youtube { color: #ff0000; }
    &.website { color: #3b82f6; }
    &.text    { color: #8b5cf6; }
}
