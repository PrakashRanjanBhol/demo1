/* FIX FOR HISTORY SCROLLBAR - ADD/MODIFY IN history.component.css */

/* MODIFIED - History content wrapper needs proper overflow */
.history-content-wrapper {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow: hidden;  /* IMPORTANT - Parent must be hidden */
    padding: 20px;
}

/* MODIFIED - History content needs to scroll */
.history-content {
    flex: 1;
    overflow-y: auto;  /* Enable scroll */
    overflow-x: hidden;
    min-height: 0;  /* Important for flex scrolling */
}

/* ADD - Scrollbar styling for history content */
.history-content::-webkit-scrollbar {
    width: 6px;
}

.history-content::-webkit-scrollbar-track {
    background: rgba(139, 92, 246, 0.05);
    border-radius: 3px;
}

.history-content::-webkit-scrollbar-thumb {
    background-color: rgba(139, 92, 246, 0.3);
    border-radius: 3px;
}

.history-content::-webkit-scrollbar-thumb:hover {
    background-color: var(--primary-color);
}
