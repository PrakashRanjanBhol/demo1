.wrapper {
  gap: 1rem;
}

.sidebar {
  background: #ffffff;
  width: 240px;
  border-right: 1px solid var(--surface-border);
  font-size: 1rem;
}

.navLabel {
  font-size: 1rem;
}

.main {
  background: #ffffff;
}
