<!-- Updated PR Header Section - Replace all .pr-header divs in your template -->

<!-- This pattern appears in multiple places: Status View (Open PRs, Merged PRs) and Activity View -->
<!-- Replace ALL occurrences of .pr-header with this updated version -->

<div class="pr-header">
  <div class="pr-state-container">
    <svg class="pr-state-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <circle cx="18" cy="18" r="3"/>
      <circle cx="6" cy="6" r="3"/>
      <path d="M13 6h3a2 2 0 0 1 2 2v7"/>
      <line x1="6" y1="9" x2="6" y2="21"/>
    </svg>
    <span class="pr-state" [class.open]="pr.state === 'OPEN'" [class.merged]="pr.state === 'MERGED'">
      {{ pr.state }}
    </span>
  </div>
  <span class="pr-id">#{{ pr.id }}</span>
</div>







/* Updated CSS for PR Header with Icon and Different State Colors */

/* Add/Update these CSS variables for state colors */
:root {
  /* ... existing variables ... */
  
  /* Open State Colors */
  --bitbucket-prs-state-open-bg: rgba(34, 197, 94, 0.15);
  --bitbucket-prs-state-open-text: #22c55e;
  --bitbucket-prs-state-open-icon: #22c55e;
  
  /* Merged State Colors */
  --bitbucket-prs-state-merged-bg: rgba(139, 92, 246, 0.15);
  --bitbucket-prs-state-merged-text: #8b5cf6;
  --bitbucket-prs-state-merged-icon: #8b5cf6;
}

/* Light Mode State Colors */
[data-theme="light"] {
  /* ... existing variables ... */
  
  /* Open State Colors */
  --bitbucket-prs-state-open-bg: rgba(34, 197, 94, 0.1);
  --bitbucket-prs-state-open-text: #16a34a;
  --bitbucket-prs-state-open-icon: #16a34a;
  
  /* Merged State Colors */
  --bitbucket-prs-state-merged-bg: rgba(139, 92, 246, 0.1);
  --bitbucket-prs-state-merged-text: #7c3aed;
  --bitbucket-prs-state-merged-icon: #7c3aed;
}

/* PR Header Styles */
.pr-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 8px;
}

.pr-state-container {
  display: flex;
  align-items: center;
  gap: 8px;
}

.pr-state-icon {
  width: 18px;
  height: 18px;
  flex-shrink: 0;
  transition: all 0.2s;
}

/* Icon colors for different states */
.pr-state.open ~ .pr-state-icon,
.pr-state-container .pr-state.open + .pr-state-icon,
.pr-state.open {
  color: var(--bitbucket-prs-state-open-icon);
}

/* Since the icon comes before the state in HTML, we need this approach */
.pr-state-container:has(.pr-state.open) .pr-state-icon {
  stroke: var(--bitbucket-prs-state-open-icon);
}

.pr-state-container:has(.pr-state.merged) .pr-state-icon {
  stroke: var(--bitbucket-prs-state-merged-icon);
}

.pr-state {
  display: inline-flex;
  align-items: center;
  padding: 4px 10px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  transition: all 0.2s;
}

.pr-state.open {
  background: var(--bitbucket-prs-state-open-bg);
  color: var(--bitbucket-prs-state-open-text);
}

.pr-state.merged {
  background: var(--bitbucket-prs-state-merged-bg);
  color: var(--bitbucket-prs-state-merged-text);
}

.pr-id {
  font-size: 11px;
  color: var(--bitbucket-prs-text-muted);
  font-weight: 500;
}

/* Hover effect for the entire card - icon should animate too */
.pr-card:hover .pr-state-icon {
  transform: scale(1.1);
}
