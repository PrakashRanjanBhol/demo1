import React from 'react';
import styles from './UploadOverlay.module.css';

const UploadOverlay = () => {
    return (
        <div className={styles.overlay}>
            <div className={styles.characterWrapper}>
                <div className={styles.character}>🧑‍💼</div>
                <div className={styles.messageBubble}>
                    Please stay with me 😄<br />
                    Your upload is in progress and will complete soon!
                </div>
            </div>
        </div>
    );
};

export default UploadOverlay;



.overlay {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    background-color: rgba(12, 12, 12, 0.95);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
    color: #fff;
    font-family: 'Segoe UI', sans-serif;
}

.characterWrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    animation: walkIn 1.5s ease-out forwards;
    opacity: 0;
}

.character {
    font-size: 4rem;
    margin-bottom: 1rem;
    animation: bounce 1s infinite alternate;
}

.messageBubble {
    background-color: #1f2937;
    border: 2px solid #3b82f6;
    border-radius: 10px;
    padding: 1rem 1.5rem;
    font-size: 1rem;
    text-align: center;
    max-width: 300px;
    box-shadow: 0 0 10px rgba(0, 123, 255, 0.3);
}

/* Entry animation for the whole character+message block */
@keyframes walkIn {
    0% {
        transform: translateX(-100%);
        opacity: 0;
    }
    100% {
        transform: translateX(0);
        opacity: 1;
    }
}

/* Bounce animation for character */
@keyframes bounce {
    0% {
        transform: translateY(0);
    }
    100% {
        transform: translateY(-8px);
    }
}


{isUploading && <UploadOverlay />}
