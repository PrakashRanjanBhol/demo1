/* Markdown Styles */
.description-content {
  font-size: 14px;
  line-height: 1.7;
  color: var(--sdlc-details-color-description);
}

.description-content > :first-child {
  margin-top: 0;
}

.description-content > :last-child {
  margin-bottom: 0;
}

.description-content h1,
.description-content h2,
.description-content h3,
.description-content h4,
.description-content h5,
.description-content h6 {
  color: var(--sdlc-details-color-title);
  margin: 20px 0 12px 0;
  font-weight: 600;
  line-height: 1.3;
}

.description-content h1:first-child,
.description-content h2:first-child,
.description-content h3:first-child {
  margin-top: 0;
}

.description-content h1 { 
  font-size: 20px;
  margin-top: 24px;
}

.description-content h2 { 
  font-size: 18px;
  margin-top: 22px;
}

.description-content h3 { 
  font-size: 16px;
}

.description-content h4 { 
  font-size: 15px;
}

.description-content h5,
.description-content h6 { 
  font-size: 14px;
}

.description-content p {
  margin: 12px 0;
  line-height: 1.7;
}

.description-content ul,
.description-content ol {
  margin: 12px 0;
  padding-left: 28px;
  line-height: 1.7;
}

.description-content ul {
  list-style-type: disc;
}

.description-content ol {
  list-style-type: decimal;
}

.description-content li {
  margin: 6px 0;
  padding-left: 4px;
}

.description-content li > ul,
.description-content li > ol {
  margin: 8px 0;
}

.description-content li > p {
  margin: 4px 0;
}

.description-content code {
  background: var(--sdlc-details-bg-section);
  padding: 3px 6px;
  border-radius: 4px;
  font-family: 'Courier New', Consolas, Monaco, monospace;
  font-size: 13px;
  border: 1px solid var(--sdlc-details-border-section);
  line-height: 1.4;
}

.description-content pre {
  background: var(--sdlc-details-bg-section);
  padding: 16px;
  border-radius: 6px;
  overflow-x: auto;
  border: 1px solid var(--sdlc-details-border-section);
  margin: 16px 0;
  line-height: 1.5;
}

.description-content pre code {
  background: none;
  padding: 0;
  border: none;
  font-size: 13px;
}

.description-content blockquote {
  border-left: 3px solid var(--sdlc-details-border-badge);
  padding: 12px 0 12px 16px;
  margin: 16px 0;
  color: var(--sdlc-details-color-description);
  font-style: italic;
  line-height: 1.6;
}

.description-content blockquote > :first-child {
  margin-top: 0;
}

.description-content blockquote > :last-child {
  margin-bottom: 0;
}

.description-content a {
  color: var(--sdlc-details-color-badge);
  text-decoration: none;
  border-bottom: 1px solid transparent;
  transition: border-color 0.2s ease;
}

.description-content a:hover {
  border-bottom-color: var(--sdlc-details-color-badge);
}

.description-content hr {
  border: none;
  border-top: 1px solid var(--sdlc-details-border-section);
  margin: 20px 0;
}

.description-content table {
  border-collapse: collapse;
  width: 100%;
  margin: 16px 0;
}

.description-content th,
.description-content td {
  border: 1px solid var(--sdlc-details-border-section);
  padding: 10px 14px;
  text-align: left;
  line-height: 1.5;
}

.description-content th {
  background: var(--sdlc-details-bg-section);
  font-weight: 600;
}

.description-content img {
  max-width: 100%;
  height: auto;
  margin: 16px 0;
  border-radius: 6px;
}

.description-content strong {
  font-weight: 600;
  color: var(--sdlc-details-color-title);
}

.description-content em {
  font-style: italic;
}

/* Ensure proper spacing between different elements */
.description-content h1 + p,
.description-content h2 + p,
.description-content h3 + p,
.description-content h4 + p,
.description-content h5 + p,
.description-content h6 + p {
  margin-top: 8px;
}

.description-content p + h1,
.description-content p + h2,
.description-content p + h3 {
  margin-top: 24px;
}

.description-content ul + p,
.description-content ol + p,
.description-content pre + p,
.description-content blockquote + p {
  margin-top: 16px;
}







.description-section {
  background: var(--sdlc-details-bg-section);
  border-radius: 10px;
  padding: 24px;
  border: 1px solid var(--sdlc-details-border-section);
}
