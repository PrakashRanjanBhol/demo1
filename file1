<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meaningful Card Animations</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            width: 80%;
        }

        .card {
            position: relative;
            height: 160px;
            border-radius: 12px;
            color: white;
            font-size: 18px;
            font-weight: bold;
            overflow: hidden;
            padding: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            cursor: pointer;
            transition: transform 0.3s ease-in-out;
        }

        .card:hover {
            transform: scale(1.03);
        }

        .card h3 {
            margin: 0;
        }

        /* Playground Card */
        .card.chat-playground {
            background: linear-gradient(135deg, #6a11cb, #2575fc);
        }

        .chat-timeline,
        .feedback-timeline {
            position: relative;
            display: flex;
            flex-direction: column;
            gap: 10px;
            padding: 15px 0;
            height: 90%;
            overflow-y: auto;
        }

        .message,
        .feedback-item {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            padding: 8px 12px;
            font-size: 12px;
            color: white;
            width: 80%;
            animation: slideIn 1.5s ease forwards;
            opacity: 0;
        }

        .message.user {
            align-self: flex-start;
        }

        .message.ai {
            align-self: flex-end;
            background: rgba(0, 0, 0, 0.3);
        }

        .typing,
        .feedback-typing {
            position: absolute;
            left: 15px;
            bottom: 15px;
            font-family: monospace;
            font-size: 12px;
            color: rgba(255, 255, 255, 0.7);
        }

        .dots {
            display: inline-flex;
            gap: 5px;
        }

        .dot {
            width: 6px;
            height: 6px;
            background: white;
            border-radius: 50%;
            animation: blink 1.5s infinite;
        }

        .dot:nth-child(1) {
            animation-delay: 0s;
        }

        .dot:nth-child(2) {
            animation-delay: 0.3s;
        }

        .dot:nth-child(3) {
            animation-delay: 0.6s;
        }

        @keyframes slideIn {
            from {
                transform: translateY(20px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes blink {

            0%,
            100% {
                opacity: 0.2;
            }

            50% {
                opacity: 1;
            }
        }

        /* Feedback Card */
        .card.feedback {
            background: linear-gradient(135deg, #00b09b, #96c93d);
        }

        .card.feedback:hover::after {
            content: "";
            position: absolute;
            top: 0;
            left: -75%;
            width: 50%;
            height: 100%;
            background: rgba(255, 255, 255, 0.2);
            transform: skewX(-25deg);
            animation: shine 0.75s forwards;
        }

        @keyframes shine {
            0% {
                left: -75%;
            }

            100% {
                left: 125%;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <!-- Playground Card -->
        <div class="card chat-playground">
            <h3>Playground</h3>
            <div class="chat-timeline">
                <div class="message user">Hi there!</div>
                <div class="message ai">Hello! How can I assist?</div>
                <div class="message user">Tell me a joke</div>
            </div>
            <div class="typing">
                AI is typing
                <div class="dots">
                    <div class="dot"></div>
                    <div class="dot"></div>
                    <div class="dot"></div>
                </div>
            </div>
        </div>

        <!-- Feedback Card -->
        <div class="card feedback">
            <h3>Feedback</h3>
            <div class="feedback-timeline">
                <div class="feedback-item">“Dark mode is great!”</div>
                <div class="feedback-item">“Need a better upload UI.”</div>
            </div>
            <div class="feedback-typing">
                Collecting responses
                <div class="dots">
                    <div class="dot"></div>
                    <div class="dot"></div>
                    <div class="dot"></div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
