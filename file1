<!-- ❌ OLD - WRONG -->
<div *ngFor="let projectKey of ...">
    (click)="showFavoriteRepositories(projectKey)"
    {{ getProjectByKey(projectKey)?.project_name || projectKey }}
</div>

<!-- ✅ NEW - CORRECT -->
<div class="project-card" 
     *ngFor="let project of favoriteProjectsList; trackBy: trackByFavoriteProjectKey"
     (click)="showFavoriteRepositories(project.project_key)">
    <div class="project-name">{{ project.project_name }}</div>
    <div class="repo-count">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
        </svg>
        {{ project.repoCount }} favorites
    </div>
</div>
