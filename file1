/* Failure message */
    .failureWrapper {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: calc(100% - 120px);
        /* adjust if needed to account for header + search */
        text-align: center;
        animation: fadeIn 0.5s ease-in-out;
        padding: 1rem;
    }


    .failureText {
        display: block;
        margin-bottom: 1rem;
        color: #ff7f7f;
        font-size: 1rem;
        font-weight: 500;
    }

    .retryButton {
        background-color: #ff4d4f !important;
        color: white;
        font-size: 0.85rem;
        padding: 0.5rem 1.2rem;
        border-radius: 6px;
        animation: bounce 0.5s ease;
    }

    /* Subtle fade in */
    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(5px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Bounce retry button */
    @keyframes bounce {
        0% {
            transform: scale(0.95);
        }

        50% {
            transform: scale(1.05);
        }

        100% {
            transform: scale(1);
        }
    }



) : !failed ? (
                    <div className={styles.failureWrapper}>
                        <span className={styles.failureText}>😕 Oops! Something went wrong.</span>
                        <Button
                            label="Try Again"
                            icon="pi pi-refresh"
                            className={styles.retryButton}
                            onClick={handleRetry}
                        />
                    </div>
                ) : (
