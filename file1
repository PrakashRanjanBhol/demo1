<div class="dots-loader">
    <span></span>
    <span></span>
    <span></span>
</div>
<p class="loader-text">Loading your settings...</p>




.dots-loader {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 120px;
}

.dots-loader span {
    width: 12px;
    height: 12px;
    margin: 0 6px;
    background: #42a5f5;
    border-radius: 50%;
    animation: bounce 0.6s infinite alternate;
}

.dots-loader span:nth-child(2) {
    animation-delay: 0.2s;
}

.dots-loader span:nth-child(3) {
    animation-delay: 0.4s;
}

@keyframes bounce {
    to {
        opacity: 0.3;
        transform: translateY(-10px);
    }
}

.loader-text {
    text-align: center;
    font-size: 1.1rem;
    font-weight: 500;
    color: #6b7280;
    margin-top: -20px;
    animation: pulseText 1.5s infinite;
}

@keyframes pulseText {

    0%,
    100% {
        opacity: 0.6;
    }

    50% {
        opacity: 1;
    }
}
