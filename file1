<div class="sidebar-container">
  <div class="d-flex">
    
    <!-- Code RAG -->
    <div class="sidebar-header"
      [class.active]="activeSection === 'rag'"
      [style.flexBasis]="activeSection === 'rag' ? '80%' : '20%'"
      (click)="setSection('rag')">

      <span *ngIf="activeSection === 'rag'" class="icon-wrapper">
        <span class="glow-circle"></span>
        <span class="icon">📦</span>
      </span>
      <span *ngIf="activeSection !== 'rag'" class="icon">📦</span>

      <span class="label" *ngIf="activeSection === 'rag'">Code RAG</span>

      <!-- Right arrow for non-active -->
      <i *ngIf="activeSection !== 'rag'" class="pi pi-chevron-right sidebar-arrow"></i>
    </div>

    <!-- Reusable -->
    <div class="sidebar-header"
      [class.active]="activeSection === 'reusable'"
      [style.flexBasis]="activeSection === 'reusable' ? '80%' : '20%'"
      (click)="setSection('reusable')">

      <!-- Left arrow for non-active -->
      <i *ngIf="activeSection !== 'reusable'" class="pi pi-chevron-left sidebar-arrow"></i>

      <span *ngIf="activeSection === 'reusable'" class="icon-wrapper">
        <span class="glow-circle"></span>
        <span class="icon">♻️</span>
      </span>
      <span *ngIf="activeSection !== 'reusable'" class="icon">♻️</span>

      <span class="label" *ngIf="activeSection === 'reusable'">Reusable</span>
    </div>
  </div>

  <div class="sidebar-body">
    <ng-container *ngIf="activeSection === 'rag'">
      <p>Tree for Code RAG</p>
    </ng-container>

    <ng-container *ngIf="activeSection === 'reusable'">
      <p>Tree for Reusable Components</p>
    </ng-container>
  </div>
</div>



















.sidebar-container {
  width: 20rem; /* 320px */
  height: 100vh;
  background-color: #111113;
  display: flex;
  flex-direction: column;
  color: white;
  font-family: 'Segoe UI', sans-serif;
  font-size: 0.875rem; /* 14px */
  overflow-y: auto;
  user-select: none;
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}

/* Base sidebar-header styles */
.sidebar-header {
  position: relative;
  display: flex;
  align-items: center;
  gap: 0.5rem; /* 8px */
  padding: 0.8rem 1rem; /* 10px 16px */
  cursor: pointer;
  transition: color 0.3s ease, transform 0.3s ease, justify-content 0.3s ease;
  font-family: 'Segoe UI', sans-serif;
  font-size: 1rem; /* 14px */
  line-height: 1.4;
  color: #b0b8c4;
  user-select: none;
  justify-content: flex-start; /* default left-aligned */
}

/* Inactive tabs left-aligned */
.sidebar-header:not(.active) {
  justify-content: flex-start;
  color: #b0b8c4;
}

/* Active tabs center-aligned */
.sidebar-header.active {
  justify-content: center;
  color: transparent;
  background-clip: text;
  -webkit-background-clip: text;
  background-image: linear-gradient(90deg, #4da0ff, #1e88e5);
  transform: scale(1.02);
}

/* Underline animation (center to full width) */
.sidebar-header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  height: 0.125rem; /* 2px */
  background: linear-gradient(90deg, #4da0ff, #1e88e5);
  width: 0%;
  transform: translateX(-50%);
  transition: width 0.4s ease;
  border-radius: 0.0625rem; /* 1px */
}

.sidebar-header.active::after {
  width: 100%;
}

/* Icon styles */
.sidebar-header i {
  font-size: 1rem; /* 16px */
  flex-shrink: 0;
  transition: color 0.3s ease;
  color: inherit; /* inherit parent color */
}

/* Active icon color */
.sidebar-header.active i {
  color: #4da0ff;
}
