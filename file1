/* Sidebar - Modified */
.sidebar {
  width: 320px;
  background: var(--agentic-ai-function-usage-bg-primary);
  border-radius: 12px;
  box-shadow: 0 2px 8px var(--agentic-ai-function-usage-shadow);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  max-height: 100vh; /* Adjust this based on your layout needs */
}

/* Guide Header - Modified */
.guide-header {
  background: var(--agentic-ai-function-usage-header-bg);
  padding: 18px 20px;
  color: var(--agentic-ai-function-usage-header-text);
  border-bottom: 1px solid var(--agentic-ai-function-usage-border);
  flex-shrink: 0; /* Prevents header from shrinking */
}

/* Guide Content - Modified */
.guide-content {
  padding: 24px 20px;
  overflow-y: auto;
  flex: 1;
}

/* Custom Scrollbar - New */
.guide-content::-webkit-scrollbar {
  width: 6px;
}

.guide-content::-webkit-scrollbar-track {
  background: transparent;
}

.guide-content::-webkit-scrollbar-thumb {
  background: var(--agentic-ai-function-usage-border);
  border-radius: 3px;
}

.guide-content::-webkit-scrollbar-thumb:hover {
  background: var(--agentic-ai-function-usage-text-secondary);
}

/* Firefox scrollbar */
.guide-content {
  scrollbar-width: thin;
  scrollbar-color: var(--agentic-ai-function-usage-border) transparent;
}
