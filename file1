<p-sidebar [(visible)]="visible" position="right" [modal]="true" [style]="{width:'550px'}">
    <!-- Header -->
    <div class="sidebar-header">
        <h3 class="sidebar-title">⚡ Live Preview</h3>
        <div class="sidebar-actions">
            <button pButton type="button" icon="pi pi-play" class="icon-btn play" pTooltip="Run Code"></button>
            <button pButton type="button" icon="pi pi-trash" class="icon-btn clear" pTooltip="Clear Code"></button>
        </div>
    </div>

    <!-- Code Areas -->
    <div class="code-section" *ngFor="let block of codeBlocks">
        <label class="code-label">{{ block.label }}</label>
        <textarea pInputTextarea [(ngModel)]="block.value" rows="6" [autoResize]="true"
            class="code-textarea"></textarea>
    </div>
</p-sidebar>

<!-- Button to open sidebar -->
<button pButton type="button" label="Open Live Preview" icon="pi pi-eye" (click)="visible = true"></button>











.p-sidebar {
    background: #111113;
    color: #f5f5f7;
    font-family: 'Poppins', sans-serif;
    padding: 1rem 1.2rem;
    border-left: 1px solid rgba(255, 255, 255, 0.08);
}

/* HEADER */
.sidebar-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.8rem 1rem;
    margin: -1rem -1.2rem 1rem;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.2), rgba(16, 185, 129, 0.15));
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    border-top-left-radius: 10px;
    border-top-right-radius: 10px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
    animation: slideDown 0.6s ease;
}

.sidebar-title {
    font-size: 1.3rem;
    font-weight: 600;
    letter-spacing: 0.5px;
    background: linear-gradient(90deg, #6366f1, #10b981);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

/* ICON BUTTONS */
.sidebar-actions {
    display: flex;
    gap: 0.6rem;
}

.icon-btn {
    width: 2.3rem;
    height: 2.3rem;
    border-radius: 50%;
    border: none;
    background: rgba(255, 255, 255, 0.07);
    color: #f5f5f7;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 0 8px rgba(0, 0, 0, 0.3);
}

.icon-btn:hover {
    transform: scale(1.15) rotate(5deg);
}

.icon-btn.play {
    color: #10b981;
    box-shadow: 0 0 10px #10b981;
}

.icon-btn.clear {
    color: #ef4444;
    box-shadow: 0 0 10px #ef4444;
}

/* CODE SECTIONS */
.code-section {
    margin-bottom: 1.3rem;
    animation: fadeIn 0.7s ease;
}

.code-label {
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 0.4rem;
    display: block;
    color: #9ca3af;
}

.code-textarea {
    width: 100%;
    border-radius: 10px;
    border: 1px solid #2d2e36;
    background: #1a1b1f;
    color: #f5f5f7;
    padding: 0.9rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    transition: all 0.3s ease;
    resize: none;
}

.code-textarea:focus {
    border-color: #6366f1;
    box-shadow: 0 0 10px #6366f1;
}

/* Animations */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(8px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes slideDown {
    from {
        opacity: 0;
        transform: translateY(-12px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}









import { Component } from '@angular/core';

@Component({
  selector: 'app-code-playground',
  templateUrl: './code-playground.component.html',
  styleUrls: ['./code-playground.component.scss']
})
export class CodePlaygroundComponent {
  visible: boolean = false;

  codeBlocks = [
    { label: 'HTML Code', value: '' },
    { label: 'CSS Code', value: '' },
    { label: 'JS Code', value: '' }
  ];
}
