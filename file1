.action-options-row.expanded {
    max-height: 160px;  /* increase to fit 2 rows */
    opacity: 1;
    padding: 10px 14px;
    border-top: 1px solid var(--border-color);
}

/* Make the whole row a column stack */
.action-options-row {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 8px;
    padding: 0 14px;
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1),
                opacity     0.3s ease,
                padding     0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border-top: 0px solid transparent;
}

/* Header sits on its own line */
.options-header {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-shrink: 0;
    width: 100%;
}

/* Remove the vertical divider since header is now its own row */
.options-header-divider {
    display: none;
}

/* Cards wrap freely */
.option-cards-row {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 6px;
    width: 100%;
    padding: 0;
    overflow: visible;
}

/* Each card takes only as much space as it needs */
.option-card {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 5px 9px;
    background: var(--bg-color);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
    white-space: nowrap;
    flex-shrink: 0;
    animation: cardSlideIn 0.25s ease-out backwards;
    flex: 0 0 auto;
}
