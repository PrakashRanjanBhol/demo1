<div class="sidebar-container">
  <div class="sidebar-header-wrapper">
    <!-- Code RAG -->
    <div class="sidebar-header" [class.active]="activeSection === 'rag'"
      [style.flexBasis]="activeSection === 'rag' ? '80%' : '20%'" (click)="setSection('rag')">
      <span *ngIf="activeSection === 'rag'" class="icon-wrapper">
        <span class="glow-circle"></span>
        <span class="icon">📦</span>
      </span>
      <span *ngIf="activeSection !== 'rag'" class="icon">📦</span>

      <span class="label" *ngIf="activeSection === 'rag'">Code RAG</span>
    </div>

    <!-- Reusable -->
    <div class="sidebar-header" [class.active]="activeSection === 'reusable'"
      [style.flexBasis]="activeSection === 'reusable' ? '80%' : '20%'" (click)="setSection('reusable')">
      <span *ngIf="activeSection === 'reusable'" class="icon-wrapper">
        <span class="glow-circle"></span>
        <span class="icon">♻️</span>
      </span>
      <span *ngIf="activeSection !== 'reusable'" class="icon">♻️</span>

      <span class="label" *ngIf="activeSection === 'reusable'">Reusable</span>
    </div>
  </div>

  <div class="sidebar-body">
    <ng-container *ngIf="activeSection === 'rag'">
      <p>Tree for Code RAG</p>
    </ng-container>

    <ng-container *ngIf="activeSection === 'reusable'">
      <p>Tree for Reusable Components</p>
    </ng-container>
  </div>
</div>





.sidebar-container {
  width: 380px;
  height: 100%;
  display: flex;
  flex-direction: column;
  background-color: #111;
  color: white;
  font-family: sans-serif;
  font-size: 0.85rem; // Base font size
}

.sidebar-header-wrapper {
  display: flex;
  height: 3.5rem; // 56px
  background: #1e1e1e;
  border-bottom: 1px solid #333;
}

.sidebar-header {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.4rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  overflow: hidden;
  position: relative;
  font-size: 0.95rem;
}

.sidebar-header.active {
  background: #2b2b2b;
}

.sidebar-header .icon {
  font-size: 1.25rem; // ~20px
  z-index: 1;
}

.sidebar-header .label {
  display: flex;
  align-items: center;
  white-space: nowrap;
  font-size: 0.95rem;
}

.sidebar-body {
  flex: 1;
  overflow-y: auto;
  padding: 1rem;
  font-size: 0.85rem;
}

/* === Static Glow Effect === */
.icon-wrapper {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.glow-circle {
  position: absolute;
  width: 1.75rem; // 28px
  height: 1.75rem;
  border-radius: 50%;
  background-color: rgba(0, 153, 255, 0.12); // Blue soft background
  box-shadow: 0 0 6px 3px rgba(0, 153, 255, 0.35); // Soft glow
  z-index: 0;
}


import { Component } from '@angular/core';

@Component({
  selector: 'app-sidebar',
  templateUrl: './sidebar.component.html',
  styleUrls: ['./sidebar.component.scss'],
})
export class SidebarComponent {
  activeSection: 'rag' | 'reusable' = 'rag';

  setSection(section: 'rag' | 'reusable') {
    this.activeSection = section;
  }
}


