code = code.replace(/subgraph\s+([^\n]*)/g, (match, p1) => {
                // Remove parentheses from the subgraph label
                const sanitized = p1.replace(/[()]/g, '');
                return `subgraph ${sanitized}`;
            });
