const handleToggle = (file_job_id: string) => {
    const newId = selectedJobId === file_job_id ? null : file_job_id;
    setSelectedJobId(newId);
    dispatch(setExcelFile({ file_job_id: newId, isFileSelected: newId !== null }));
    setError(null);
    setWarning(null);
  };
