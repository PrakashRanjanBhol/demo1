.wrapper {
  gap: 1rem;
}

.sidebar {
  background: #ffffff;
  width: 300px;
  border-right: 1px solid var(--surface-border);
  font-size: 1rem;
  border-radius: 12px;
}

.navLabel {
  font-size: 1rem;
}

.main {
  background: #ffffff;
  border-radius: 12px;
}
