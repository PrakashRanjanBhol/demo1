<div class="diff-container">
  <ng-container *ngIf="!selectedFile">
    <div class="empty-state">
      <div class="empty-state-icon">📂</div>
      <h3>No File Selected</h3>
      <p>Please select a file from the dropdown above to view the diff and AI comments</p>
    </div>
  </ng-container>

  <ng-container *ngIf="selectedFile">
    <!-- No Comments Info Banner -->
    <div class="info-banner" *ngIf="!hasComments()">
      <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" class="info-icon">
        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/>
      </svg>
      <div class="info-content">
        <strong>No AI Comments</strong>
        <span>This file has no AI-generated comments or suggestions. Review the changes below.</span>
      </div>
    </div>

    <!-- Unified View -->
    <div *ngIf="currentView === 'unified'">
      <div class="file-header">📄 {{ selectedFile.file_path }}</div>
      <div class="unified-view">
        <!-- Your existing diff lines code -->
      </div>
    </div>

    <!-- Side by Side View -->
    <div *ngIf="currentView === 'side-by-side'">
      <div class="file-header">📄 {{ selectedFile.file_path }}</div>
      <div class="side-by-side-view">
        <!-- Your existing side-by-side code -->
      </div>
    </div>
  </ng-container>
</div>













.info-banner {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 18px;
  background: linear-gradient(135deg, 
    rgba(16, 185, 129, 0.1) 0%, 
    rgba(5, 150, 105, 0.05) 100%);
  border: 1.5px solid rgba(16, 185, 129, 0.3);
  border-left: 4px solid #10b981;
  border-radius: 8px;
  margin-bottom: 20px;
  animation: slideDown 0.3s ease-out;

  .info-icon {
    width: 24px;
    height: 24px;
    fill: #10b981;
    flex-shrink: 0;
  }

  .info-content {
    display: flex;
    flex-direction: column;
    gap: 4px;
    flex: 1;

    strong {
      font-size: 14px;
      font-weight: 700;
      color: var(--bitbucket-pr-review-text-primary);
    }

    span {
      font-size: 13px;
      color: var(--bitbucket-pr-review-text-tertiary);
      line-height: 1.4;
    }
  }
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 100px 40px;
  text-align: center;
  min-height: 400px;

  .empty-state-icon {
    font-size: 80px;
    margin-bottom: 24px;
    opacity: 0.6;
    animation: float 3s ease-in-out infinite;
  }

  h3 {
    font-size: 24px;
    font-weight: 700;
    color: var(--bitbucket-pr-review-text-primary);
    margin: 0 0 12px 0;
  }

  p {
    font-size: 16px;
    color: var(--bitbucket-pr-review-text-tertiary);
    max-width: 500px;
    line-height: 1.6;
    margin: 0;
  }
}

@keyframes float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-10px);
  }
}

// Dark mode adjustments
:host.dark-mode {
  .info-banner {
    background: linear-gradient(135deg, 
      rgba(16, 185, 129, 0.15) 0%, 
      rgba(5, 150, 105, 0.08) 100%);
    border-color: rgba(16, 185, 129, 0.4);
  }
}

// Responsive adjustments
@media (max-width: 768px) {
  .info-banner {
    padding: 12px 16px;
    gap: 10px;

    .info-icon {
      width: 20px;
      height: 20px;
    }

    .info-content {
      strong {
        font-size: 13px;
      }

      span {
        font-size: 12px;
      }
    }
  }

  .empty-state {
    padding: 60px 20px;
    min-height: 300px;

    .empty-state-icon {
      font-size: 60px;
    }

    h3 {
      font-size: 20px;
    }

    p {
      font-size: 14px;
    }
  }
}


