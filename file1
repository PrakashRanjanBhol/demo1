/* MODIFIED PART 1 - Remove padding from .upload-content */

/* Upload Content - REMOVE ALL PADDING */
.upload-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    padding: 0;  /* CHANGED from: padding: 20px 24px 24px 24px; */
}

/* MODIFIED PART 2 - Add gap to split columns */
.split-three-columns {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 0;  /* CHANGED from: gap: 16px; - Remove gap so borders touch */
    height: 100%;
    overflow: hidden;
}

/* MODIFIED PART 3 - Add borders to columns */
.split-three-columns > * {
    min-width: 0;
    height: 100%;
    overflow-y: auto;
    overflow-x: hidden;
    border-right: 1px solid var(--border-color);  /* ADD THIS */
}

/* Remove border from last column */
.split-three-columns > *:last-child {
    border-right: none;  /* ADD THIS */
}

/* MODIFIED PART 4 - Update responsive for mobile */
@media (max-width: 768px) {
    .upload-content {
        padding: 0;  /* CHANGED from: padding: 16px; */
    }
}












<!-- WRAP YOUR ENTIRE UPLOAD-GENERATE COMPONENT CONTENT WITH THIS STRUCTURE -->

<div class="upload-third">
    <!-- Header Section -->
    <div class="upload-section-header">
        <h3 class="upload-section-title">Upload & Generate</h3>
    </div>
    
    <!-- Content Section with internal padding -->
    <div class="upload-section-content">
        <!-- ALL YOUR EXISTING UPLOAD-GENERATE CONTENT GOES HERE -->
        <!-- Move all your current component HTML inside this div -->
        
        <!-- Example: Your existing content structure -->
        <!-- Mode toggle buttons, file upload area, etc. -->
        
    </div>
</div>


















/* ADD THESE STYLES TO upload-generate.component.css */

/* Container wrapper with internal padding */
.upload-third {
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: hidden;
}

/* Header section - full width with gradient background */
.upload-section-header {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.08), rgba(167, 139, 250, 0.08));
    border-bottom: 1px solid var(--border-color);
    padding: 16px 20px;
    flex-shrink: 0;
}

.upload-section-title {
    font-size: 16px;
    font-weight: 700;
    margin: 0;
    color: var(--text-color);
    display: flex;
    align-items: center;
    gap: 10px;
}

.upload-section-title::before {
    content: '';
    width: 4px;
    height: 20px;
    background: linear-gradient(180deg, var(--primary-color), #a78bfa);
    border-radius: 2px;
}

/* Content area with padding */
.upload-section-content {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    padding: 20px;  /* Internal padding */
}

/* Scrollbar for content area */
.upload-section-content::-webkit-scrollbar {
    width: 6px;
}

.upload-section-content::-webkit-scrollbar-track {
    background: rgba(139, 92, 246, 0.05);
    border-radius: 3px;
}

.upload-section-content::-webkit-scrollbar-thumb {
    background-color: rgba(139, 92, 246, 0.3);
    border-radius: 3px;
}

.upload-section-content::-webkit-scrollbar-thumb:hover {
    background-color: var(--primary-color);
}
















/* MODIFY/ADD THESE STYLES TO history.component.css */

/* MODIFIED - History Third container */
.history-third {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    height: 100%;
    /* Remove any existing padding here */
}

/* MODIFIED - Section header inline - make it full width with gradient */
.section-header-inline {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.08), rgba(167, 139, 250, 0.08));
    border-bottom: 1px solid var(--border-color);
    padding: 16px 20px;
    flex-shrink: 0;
    /* Remove any sticky positioning if exists */
    position: relative;
}

.header-content {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

/* MODIFIED - Section title */
.section-inline-title {
    font-size: 16px;
    font-weight: 700;
    margin: 0;
    color: var(--text-color);
    display: flex;
    align-items: center;
    gap: 10px;
}

.section-inline-title::before {
    content: '';
    width: 4px;
    height: 20px;
    background: linear-gradient(180deg, var(--primary-color), #a78bfa);
    border-radius: 2px;
}

/* MODIFIED - History content wrapper - add internal padding */
.history-content-wrapper {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    padding: 20px;  /* ADD internal padding */
}

/* Rest of your existing history styles remain the same */
/* Just ensure .history-content has no extra top padding */
.history-content {
    /* Remove any top padding/margin if exists */
    padding-top: 0;
}















/* MODIFY/ADD THESE STYLES TO workspace.component.css */

/* MODIFIED - Workspace Third container */
.workspace-third {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    height: 100%;
    padding-left: 0;  /* REMOVE the existing padding-left: 16px; */
}

/* MODIFIED - Workspace header - make it full width with gradient */
.workspace-header-inline {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.08), rgba(167, 139, 250, 0.08));
    border-bottom: 1px solid var(--border-color);
    padding: 16px 20px;
    flex-shrink: 0;
    /* Remove any sticky positioning */
    position: relative;
    margin-bottom: 0;  /* CHANGED from: margin-bottom: 20px; */
}

.workspace-header-content {
    display: flex;
    flex-direction: column;
    gap: 8px;
}

/* MODIFIED - Workspace title */
.workspace-inline-title {
    font-size: 16px;
    font-weight: 700;
    background: transparent;  /* REMOVE gradient text */
    -webkit-background-clip: unset;
    -webkit-text-fill-color: unset;
    background-clip: unset;
    color: var(--text-color);  /* ADD solid color */
    margin: 0;
    position: relative;
    padding-left: 0;  /* REMOVE padding-left: 20px; */
    letter-spacing: -0.02em;
    display: inline-flex;
    align-items: center;
    gap: 10px;
}

.workspace-inline-title::before {
    content: '';
    width: 4px;
    height: 20px;
    background: linear-gradient(180deg, var(--primary-color), #a78bfa);
    border-radius: 2px;
    /* Remove absolute positioning */
    position: relative;
    left: 0;
}

/* MODIFIED - Workspace content - add internal padding */
.workspace-content-inline {
    flex: 1;
    display: flex;
    flex-direction: column;
    overflow-y: auto;
    overflow-x: hidden;
    min-height: 0;
    padding: 20px;  /* ADD internal padding */
}

/* Selected file info stays inside the header */
.selected-file-info {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    color: var(--text-muted);
    margin-top: 0;  /* REMOVE margin-top: 12px; */
    padding: 8px 12px;
    background: rgba(139, 92, 246, 0.05);
    border-radius: 8px;
    border: 1px solid rgba(139, 92, 246, 0.1);
}
