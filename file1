.task-form {
  background: var(--sdlc-suite-bg-tertiary);
  border-radius: 1rem;
  padding: 1.5rem;
  border: 1px solid var(--sdlc-suite-border-tertiary);
  backdrop-filter: blur(10px);
  box-shadow: var(--sdlc-suite-shadow-secondary);
}

.task-form-container {
  opacity: 0;
  visibility: hidden;
  max-height: 0;
  overflow: hidden;
  transform: translateY(-0.625rem);
  transition: opacity 0.3s ease, transform 0.3s ease, max-height 0.4s ease, visibility 0s linear 0.3s, margin 0.3s ease;
  margin-top: 0;
  margin-bottom: 0;
  width: 100%;
}

.task-form-container.show {
  opacity: 1;
  visibility: visible;
  max-height: 600px;
  transform: translateY(0);
  transition: opacity 0.3s ease, transform 0.3s ease, max-height 0.4s ease, visibility 0s linear 0s, margin 0.3s ease;
  margin-top: 0;
  margin-bottom: 1.5rem;
}
