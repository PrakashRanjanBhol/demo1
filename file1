import axiosInstance from './axiosInstance';

export interface AdminSettings {
  file_size_limit: number;
  no_of_file_upload: number;
}

export interface AdminSettingsResponse {
  status: 'Success' | 'Error';
  result: AdminSettings;
}

export interface SaveAdminSettingsResponse {
  status: 'Success' | 'Error';
  result: string;
}

export const fetchAdminSettingsApi = async (): Promise<AdminSettings> => {
  const response = await axiosInstance.get<AdminSettingsResponse>('/auth/get_admin_settings');
  return response.data.result;
};

export const saveAdminSettingsApi = async (payload: AdminSettings): Promise<string> => {
  const response = await axiosInstance.post<SaveAdminSettingsResponse>(
    '/auth/save_admin_settings',
    payload
  );
  return response.data.result;
};





export const saveAdminSettings = createAsyncThunk(
  'generalSettings/save',
  async (payload: AdminSettings, { rejectWithValue }) => {
    try {
      const message = await saveAdminSettingsApi(payload);
      return { settings: payload, message };
    } catch (err: unknown) {
      const message = err instanceof Error ? err.message : 'Failed to save settings';
      return rejectWithValue(message);
    }
  }
);





.addCase(saveAdminSettings.fulfilled, (state, action) => {
  state.saving = false;
  state.saveSuccess = true;
  state.settings = action.payload.settings;
})
