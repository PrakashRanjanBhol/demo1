/* Completed Badge - Green */
.vibe-coding-completed-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 10px;
  background: var(--vibe-completed-bg);
  color: var(--vibe-completed-text);
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
  letter-spacing: 0.3px;
  border: 1px solid var(--vibe-completed-border);
}

.vibe-coding-completed-icon {
  width: 14px;
  height: 14px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--vibe-completed-icon-bg);
  color: var(--vibe-completed-icon-text);
  border-radius: 50%;
  font-size: 10px;
  font-weight: bold;
}
