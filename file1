<!-- MODIFIED - history.component.html (line 150) -->

<!-- FIND: -->
<h4 class="history-item-name">{{ item.name }}</h4>

<!-- REPLACE WITH: -->
<h4 class="history-item-name">
    <span class="item-id">#{{ item.id }}</span>
    {{ item.name }}
</h4>




<!-- MODIFIED - upload-generate.component.html -->

<!-- 1. NEW UPLOAD - Step 2 File Selection (Line 195) -->

<!-- FIND: -->
<span class="file-name-truncate">{{ fileWithStatus.file.name }}</span>

<!-- REPLACE WITH: -->
<span class="file-name-truncate">
    <span class="item-id" *ngIf="fileWithStatus.uploadedFileId">#{{ fileWithStatus.uploadedFileId }}</span>
    {{ fileWithStatus.file.name }}
</span>










<!-- MODIFIED - upload-generate.component.html -->

<!-- 2. PREVIOUS FILES - File List (Line 354) -->

<!-- FIND: -->
<div class="file-details">
    <span class="file-name">{{ file.name }}</span>
    <span class="file-date">{{ file.insertedAt }}</span>
</div>

<!-- REPLACE WITH: -->
<div class="file-details">
    <span class="file-name">
        <span class="item-id">#{{ file.id }}</span>
        {{ file.name }}
    </span>
    <span class="file-date">{{ file.insertedAt }}</span>
</div>











/* ADD TO BOTH history.component.css AND upload-generate.component.css */

/* ID Badge Styling */
.item-id {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 32px;
    height: 20px;
    padding: 0 8px;
    margin-right: 8px;
    font-size: 11px;
    font-weight: 700;
    color: var(--primary-color);
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(167, 139, 250, 0.1));
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 6px;
    letter-spacing: 0.5px;
}

/* Hover effect for ID badge */
.history-item:hover .item-id,
.summary-file-item:hover .item-id {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.15), rgba(167, 139, 250, 0.15));
    border-color: rgba(139, 92, 246, 0.3);
}

/* Selected item ID badge */
.history-item.selected .item-id,
.summary-file-item.selected .item-id {
    color: #ffffff;
    background: linear-gradient(135deg, var(--primary-color), #a78bfa);
    border-color: var(--primary-color);
}
