.file-info {
  display: flex;
  align-items: center;
  gap: 12px;
}

.file-name {
  font-size: 16px;
  font-weight: 600;
  letter-spacing: 0.3px;
  color: var(--file-name-color, #1a1a1b);
}

.file-version {
  font-size: 12px;
  font-weight: 500;
  color: var(--file-version-color, #4a5568);
  background: var(--file-version-bg, #e2e8f0);
  padding: 4px 10px;
  border-radius: 12px;
}

/* Light mode (default) */
:root {
  --file-name-color: #1a1a1b;
  --file-version-color: #4a5568;
  --file-version-bg: #e2e8f0;
}

/* Dark mode */
[data-theme="dark"],
.dark-mode,
@media (prefers-color-scheme: dark) {
  :root {
    --file-name-color: #f1f5f9;
    --file-version-color: #cbd5e1;
    --file-version-bg: rgba(148, 163, 184, 0.2);
  }
}
