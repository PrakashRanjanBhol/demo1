<!-- Updated PR Footer Section - Replace the .pr-footer div in your HTML template -->

<!-- For Status View - Inside the *ngFor loops for openPRs and mergedPRs -->
<div class="pr-footer">
  <div class="comment-count" [class.has-comments]="pr.user_comments > 0">
    <svg class="comment-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
    </svg>
    <span>{{ pr.user_comments }} comment{{ pr.user_comments !== 1 ? 's' : '' }}</span>
  </div>
  <div class="pr-actions">
    <a [href]="pr.url" class="view-link" (click)="stopPropagation($event)">
      Auto Review
    </a>
    <span class="action-separator">|</span>
    <a [href]="pr.url" class="view-link" (click)="stopPropagation($event)">
      View →
    </a>
  </div>
</div>

<!-- For Activity View - Inside the *ngFor loop for filteredAndSortedPRs -->
<div class="pr-footer">
  <div class="comment-count" [class.has-comments]="pr.user_comments > 0">
    <svg class="comment-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
    </svg>
    <span>{{ pr.user_comments }} comment{{ pr.user_comments !== 1 ? 's' : '' }}</span>
  </div>
  <div class="pr-actions">
    <a [href]="pr.url" class="view-link" (click)="stopPropagation($event)">
      Auto Review
    </a>
    <span class="action-separator">|</span>
    <a [href]="pr.url" class="view-link" (click)="stopPropagation($event)">
      View →
    </a>
  </div>
</div>

















/* Add these styles to your CSS file for the Auto Review link */

.pr-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 10px;
  border-top: 1px solid var(--bitbucket-prs-border-primary);
}

.pr-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

.action-separator {
  font-size: 11px;
  color: var(--bitbucket-prs-text-muted);
  font-weight: 400;
}

.view-link {
  font-size: 11px;
  background: var(--bitbucket-prs-accent-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-decoration: none;
  display: flex;
  align-items: center;
  gap: 4px;
  font-weight: 600;
  transition: opacity 0.2s;
}

.view-link:hover {
  opacity: 0.8;
}
