/* Sidebar */
.sidebar {
  position: fixed;
  top: 60px;
  bottom: 34px;
  right: 0;
  width: 320px;
  background-color: var(--agentic-ai-share-sidebar-bg);
  border: 1px solid var(--agentic-ai-share-sidebar-border);
  z-index: 1000;
  display: flex;
  flex-direction: column;
  transform: translateX(-320px);
  transition: transform 300ms ease-out;
}

@media (min-width: 1801px) {
  .sidebar {
    transform: translateX(-400px);
  }
}

/* Header Section - Fixed */
.sidebar-header {
  padding: 20px;
  background-color: var(--agentic-ai-share-sidebar-header-bg);
  border-bottom: 1px solid var(--agentic-ai-share-sidebar-header-border);
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 10;
}

/* Search Section - Fixed */
.search-section {
  padding: 16px 20px;
  border-bottom: 1px solid var(--agentic-ai-share-sidebar-header-border);
  flex-shrink: 0;
  background-color: var(--agentic-ai-share-sidebar-bg);
  position: sticky;
  top: 0;
  z-index: 9;
}

/* Select All Section - Fixed */
.select-all-container {
  padding: 16px 20px;
  border-bottom: 1px solid var(--agentic-ai-share-sidebar-header-border);
  background-color: var(--agentic-ai-share-sidebar-select-all-bg);
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 8;
}

/* Employee List Section - Scrollable */
.employee-list {
  flex: 1;
  overflow-y: auto;
  padding: 12px 0;
  min-height: 0;
}

/* Footer Section - Fixed */
.sidebar-footer {
  padding: 16px 20px;
  border-top: 1px solid var(--agentic-ai-share-sidebar-header-border);
  background-color: var(--agentic-ai-share-sidebar-bg);
  flex-shrink: 0;
  position: sticky;
  bottom: 0;
  z-index: 10;
}
