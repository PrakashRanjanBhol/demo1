:root {
  /* Light mode (default) */
  --adk-artifact-view-header-bg: #ffffff;
  --adk-artifact-view-header-border: #e0e0e0;
  --adk-artifact-view-file-name-color: #1a1a1b;
  --adk-artifact-view-file-version-color: #4a5568;
  --adk-artifact-view-file-version-bg: #e2e8f0;
  --adk-artifact-view-zoom-btn-bg: #ffffff;
  --adk-artifact-view-zoom-btn-border: #e2e8f0;
  --adk-artifact-view-zoom-btn-color: #4a5568;
  --adk-artifact-view-zoom-btn-hover-bg: #f8fafc;
  --adk-artifact-view-zoom-btn-hover-border: #cbd5e1;
  --adk-artifact-view-zoom-btn-hover-color: #1a1a1b;
  --adk-artifact-view-zoom-level-color: #4a5568;
  --adk-artifact-view-image-container-bg: #f5f5f5;
}

/* Dark mode */
.dark-mode,
[data-theme="dark"] {
  --adk-artifact-view-header-bg: #0a0a0b;
  --adk-artifact-view-header-border: #2a2a2b;
  --adk-artifact-view-file-name-color: #f1f5f9;
  --adk-artifact-view-file-version-color: #cbd5e1;
  --adk-artifact-view-file-version-bg: rgba(148, 163, 184, 0.2);
  --adk-artifact-view-zoom-btn-bg: #1a1a1b;
  --adk-artifact-view-zoom-btn-border: #2a2a2b;
  --adk-artifact-view-zoom-btn-color: #cbd5e1;
  --adk-artifact-view-zoom-btn-hover-bg: #2a2a2b;
  --adk-artifact-view-zoom-btn-hover-border: #3a3a3b;
  --adk-artifact-view-zoom-btn-hover-color: #f1f5f9;
  --adk-artifact-view-zoom-level-color: #cbd5e1;
  --adk-artifact-view-image-container-bg: #0a0a0b;
}






.preview-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px;
  border-bottom: 1px solid var(--adk-artifact-view-header-border);
  background: var(--adk-artifact-view-header-bg);
}

.file-info {
  display: flex;
  align-items: center;
  gap: 12px;
}

.file-name {
  font-size: 16px;
  font-weight: 600;
  letter-spacing: 0.3px;
  color: var(--adk-artifact-view-file-name-color);
}

.file-version {
  font-size: 12px;
  font-weight: 500;
  color: var(--adk-artifact-view-file-version-color);
  background: var(--adk-artifact-view-file-version-bg);
  padding: 4px 10px;
  border-radius: 12px;
}

.zoom-controls {
  display: flex;
  align-items: center;
  gap: 8px;
}

.zoom-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  border: 1px solid var(--adk-artifact-view-zoom-btn-border);
  background: var(--adk-artifact-view-zoom-btn-bg);
  border-radius: 6px;
  cursor: pointer;
  transition: all 0.2s ease;
  color: var(--adk-artifact-view-zoom-btn-color);
}

.zoom-btn:hover:not(:disabled) {
  background: var(--adk-artifact-view-zoom-btn-hover-bg);
  border-color: var(--adk-artifact-view-zoom-btn-hover-border);
  color: var(--adk-artifact-view-zoom-btn-hover-color);
}

.zoom-btn:active:not(:disabled) {
  transform: scale(0.95);
}

.zoom-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.zoom-level {
  font-size: 13px;
  font-weight: 500;
  color: var(--adk-artifact-view-zoom-level-color);
  min-width: 45px;
  text-align: center;
}

.preview-container {
  height: calc(100% - 49px);
  width: 100%;
  position: relative;
}

.preview-iframe {
  height: 100%;
  width: 100%;
  border: none;
}

.image-preview-container {
  height: 100%;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--adk-artifact-view-image-container-bg);
  position: absolute;
  top: 0;
  left: 0;
  overflow: auto;
}

.preview-image {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
  transform-origin: center center;
}

.loading-overlay {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.spinner {
  border: 4px solid #f3f3f3;
  border-top: 4px solid #3498db;
  border-radius: 50%;
  width: 40px;
  height: 40px;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.error-message {
  color: #d32f2f;
  font-family: Arial, sans-serif;
  text-align: center;
  padding: 20px;
}
