.employee-name {
  font-size: 14px;
  color: var(--agentic-ai-share-sidebar-text-color);
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.checkbox-item {
  display: flex;
  align-items: center;
  cursor: pointer;
  gap: 8px;
  min-width: 0;
}
