<!-- Replace the versions list section: -->
<div *ngIf="!loadingVersions && artifactVersions.length > 0" class="versions-list">
  <div class="version-item" *ngFor="let version of artifactVersions">
    <span class="version-name">v{{ version.version }}</span>
    <button 
      class="download-btn" 
      [disabled]="downloadingVersion === version.version"
      (click)="downloadArtifact(artifact, version); $event.stopPropagation()"
      title="Download version {{ version.version }}"
    >
      <div *ngIf="downloadingVersion === version.version" class="btn-spinner"></div>
      <svg *ngIf="downloadingVersion !== version.version" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
      </svg>
    </button>
  </div>
</div>


/* Update these styles: */
.versions-list {
    display: flex;
    flex-direction: column;
    gap: 4px; /* Changed from 8px to 4px */
}

.version-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 6px 8px; /* Changed from 10px to 6px 8px */
    background: var(--adk-agents-card-hover-bg);
    border: 1px solid var(--adk-agents-border);
    border-radius: 4px; /* Changed from 6px to 4px */
    transition: all 0.2s;
}

.version-item:hover {
    border-color: #667eea;
}

.version-info {
    display: flex;
    align-items: center;
}

.version-name {
    font-size: 12px; /* Changed from 13px to 12px */
    font-weight: 500; /* Changed from 600 to 500 */
    color: var(--adk-agents-text-primary);
}

.download-btn {
    background: transparent;
    border: none; /* Changed from 1px solid to none */
    border-radius: 4px; /* Changed from 6px to 4px */
    padding: 4px; /* Changed from 6px to 4px */
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--adk-agents-text-secondary); /* Changed color */
    transition: all 0.2s;
}

.download-btn:disabled {
    cursor: not-allowed;
    opacity: 0.6;
}

.download-btn:not(:disabled):hover {
    background: #667eea;
    color: white;
}

.download-btn svg {
    width: 14px; /* Changed from 16px to 14px */
    height: 14px; /* Changed from 16px to 14px */
}

.btn-spinner {
    width: 14px; /* Changed from 16px to 14px */
    height: 14px; /* Changed from 16px to 14px */
    border: 2px solid var(--adk-agents-border);
    border-top-color: #667eea;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
}
